# Notesmaster 修改日志

## 修改 13：修复闹钟提醒功能（5 个崩溃点全部修复）

**日期**：2026-05-31

**问题描述**：

原始 MIUI 便签的闹钟提醒功能完全不可用，共 5 个崩溃点。

**修改文件**：

| 文件 | 路径 |
|------|------|
| AndroidManifest.xml | `Notesmaster/app/src/main/AndroidManifest.xml` |
| AlarmAlertActivity.java | `Notesmaster/app/src/main/java/net/micode/notes/ui/AlarmAlertActivity.java` |
| AlarmAlertActivity 的布局文件 | `Notesmaster/app/src/main/res/layout/alarm_alert.xml` |
| AlarmInitReceiver.java | `Notesmaster/app/src/main/java/net/micode/notes/widget/AlarmInitReceiver.java` |
| AlarmReceiver.java | `Notesmaster/app/src/main/java/net/micode/notes/ui/AlarmReceiver.java` |
| DateTimePickerDialog.java | `Notesmaster/app/src/main/java/net/micode/notes/ui/DateTimePickerDialog.java` |
| GTaskManager.java | `Notesmaster/app/src/main/java/net/micode/notes/gtask/remote/GTaskManager.java` |
| NoteEditActivity.java | `Notesmaster/app/src/main/java/net/micode/notes/ui/NoteEditActivity.java` |
| NotesListActivity.java | `Notesmaster/app/src/main/java/net/micode/notes/ui/NotesListActivity.java` |

**修改内容**：

### 1. `AlarmAlertActivity`：从废弃 API 迁移到 Notification + FullScreenIntent

原代码用 `KeyguardLock`（API 17 废弃）、`WakeLock` 等问题，直接改为：
- 高优先级 Notification 弹窗
- 使用 `setFullScreenIntent` 弹出全屏 AlarmAlertActivity
- 锁屏状态下正常显示

### 2. `AlarmReceiver`：使用 NotificationChannel

- API 26+ 必须创建 `NotificationChannel`，否则通知不显示
- 使用 `note_alarm_channel`，优先级 IMPORTANCE_HIGH

### 3. `DateTimePickerDialog`：从 `TimePickerDialog` 升级到完整日期时间选择

- 添加日期选择功能
- UI 调整适配

### 4. `AlarmInitReceiver`：BootReceiver 恢复正常

- 设备重启后自动恢复所有未过期闹钟

### 5. `AlarmAlertActivity`：DataExtractionRules 兼容

- API 31+ 需要处理 DataExtractionRules

**效果**：

- 闹钟设置成功注册到系统 AlarmManager（精确定时，Doze 不延迟）
- 闹钟到期时通过高优先级 Notification + 全屏 Intent 弹出 `AlarmAlertActivity`
- 锁屏状态下可唤醒屏幕并显示闹钟弹窗
- 设备重启后 `AlarmInitReceiver` 自动恢复所有未过期闹钟
- 点击通知或全屏弹窗中的按钮可进入对应笔记

---

## 修改 14：添加闹钟权限运行时请求（API 33-36 兼容）

**日期**：2026-05-31

**问题描述**：

修改 13 虽然修复了闹钟的代码逻辑，但在 API 36.1 模拟器上仍然无法弹出闹钟。原因是三个关键权限仅声明在 `AndroidManifest.xml` 中，但从未在代码中请求：
- `POST_NOTIFICATIONS` — API 33+ 运行时权限，需用户弹窗授权
- `SCHEDULE_EXACT_ALARM` — API 31+ 特殊权限，需用户到系统设置手动开启
- `USE_FULL_SCREEN_INTENT` — API 34+ 特殊权限，需用户到系统设置手动开启

这三个中任意一个缺失，闹钟界面都无法弹出。

**修改文件**：

| 文件 | 路径 |
|------|------|
| NoteEditActivity.java | `Notesmaster/app/src/main/java/net/micode/notes/ui/NoteEditActivity.java` |

**修改内容**：

### 1. 新增 imports

```diff
+import android.Manifest;
+import android.app.NotificationManager;
+import android.content.pm.PackageManager;
+import android.net.Uri;
+import android.os.Build;
+import android.provider.Settings;
```

### 2. 新增请求码常量

```java
private static final int REQUEST_CODE_POST_NOTIFICATIONS = 100;
```

### 3. `setReminder()` — 前置权限检查

```diff
 private void setReminder() {
+    if (!checkAlarmPermissions()) {
+        return;
+    }
     DateTimePickerDialog d = new DateTimePickerDialog(this, ...);
     ...
 }
```

### 4. 新增 `checkAlarmPermissions()` — 三层检查

```java
private boolean checkAlarmPermissions() {
    // 检查 1: POST_NOTIFICATIONS (API 33+)
    if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.TIRAMISU) {
        if (checkSelfPermission(Manifest.permission.POST_NOTIFICATIONS)
                != PackageManager.PERMISSION_GRANTED) {
            requestPermissions(new String[]{Manifest.permission.POST_NOTIFICATIONS},
                    REQUEST_CODE_POST_NOTIFICATIONS);
            return false;
        }
    }

    // 检查 2: SCHEDULE_EXACT_ALARM (API 31+)
    if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.S) {
        AlarmManager am = (AlarmManager) getSystemService(Context.ALARM_SERVICE);
        if (!am.canScheduleExactAlarms()) {
            showPermissionGuideDialog(..., Settings.ACTION_REQUEST_SCHEDULE_EXACT_ALARM);
            return false;
        }
    }

    // 检查 3: USE_FULL_SCREEN_INTENT (API 34+)
    if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.UPSIDE_DOWN_CAKE) {
        NotificationManager nm = ...;
        if (!nm.canUseFullScreenIntent()) {
            showPermissionGuideDialog(..., Settings.ACTION_MANAGE_APP_USE_FULL_SCREEN_INTENT);
            return false;
        }
    }

    return true;
}
```

### 5. 新增 `showPermissionGuideDialog()` — 引导到系统设置

对无法通过代码直接请求的特殊权限（`SCHEDULE_EXACT_ALARM`、`USE_FULL_SCREEN_INTENT`），弹窗引导用户到系统设置页面手动开启：

```java
private void showPermissionGuideDialog(String permissionName, String message,
        final String settingsAction) {
    AlertDialog.Builder builder = new AlertDialog.Builder(this);
    builder.setTitle("Permission Required");
    builder.setMessage(message);
    builder.setPositiveButton("Open Settings", ...);
    builder.setNegativeButton(android.R.string.cancel, null);
    builder.show();
}
```

### 6. 新增 `onRequestPermissionsResult()` — 处理运行时权限回调

```java
@Override
public void onRequestPermissionsResult(int requestCode, String[] permissions,
        int[] grantResults) {
    super.onRequestPermissionsResult(requestCode, permissions, grantResults);
    if (requestCode == REQUEST_CODE_POST_NOTIFICATIONS) {
        if (grantResults.length > 0
                && grantResults[0] == PackageManager.PERMISSION_GRANTED) {
            setReminder();  // 权限通过，重新打开时间选择器
        }
    }
}
```

**用户交互流程 (API 36)**：

```
用户点击 "提醒我"
  → checkAlarmPermissions()
      ├─ POST_NOTIFICATIONS 未授权？
      │    → 系统弹窗 "允许 Notes 发送通知？"
      │         → [允许] → 重新进入 checkAlarmPermissions()
      │         → [拒绝] → 返回
      ├─ SCHEDULE_EXACT_ALARM 未授权？
      │    → 弹窗 "请在设置中开启 Alarms & reminders"
      │         → [打开设置] → 系统设置页 → 用户手动开启
      │         → [取消] → 返回
      └─ USE_FULL_SCREEN_INTENT 未授权？
           → 弹窗 "请在设置中开启 Full-screen notifications"
                → [打开设置] → 系统设置页 → 用户手动开启
                → [取消] → 返回
  → 全部通过 → 显示时间选择器 → 设置闹钟 ✅
```

**效果**：

- 首次点击 "提醒我" 时自动引导用户完成所有三个权限的授权
- `POST_NOTIFICATIONS` 走标准运行时权限弹窗
- `SCHEDULE_EXACT_ALARM` 和 `USE_FULL_SCREEN_INTENT` 走引导弹窗 → 系统设置
- 权限全部就位后正常设置闹钟，闹钟到期时全屏弹窗通知

---

## 修改 15：添加 CallReceiver 监听系统通话，自动创建通话便签

**日期**：2026-05-31

**问题描述**：

通话便签依赖于 MIUI 系统电话应用在挂断后发送 `ACTION_INSERT_OR_EDIT` Intent，但标准 Android 拨号器（包括模拟器）不发送此 Intent。导致模拟来电后无法自动创建通话便签，"通话便签"文件夹始终为空。

**方案**：新增 `CallReceiver`（BroadcastReceiver），监听系统 `PHONE_STATE` 和 `NEW_OUTGOING_CALL` 广播，通话结束后自动创建通话便签。

**修改文件**：

| 文件 | 路径 | 操作 |
|------|------|------|
| CallReceiver.java | `Notesmaster/app/src/main/java/net/micode/notes/ui/CallReceiver.java` | **新建** |
| AndroidManifest.xml | `Notesmaster/app/src/main/AndroidManifest.xml` | 权限 + Receiver 注册 |
| NotesListActivity.java | `Notesmaster/app/src/main/java/net/micode/notes/ui/NotesListActivity.java` | 运行时权限请求 |

**修改内容**：

### 1. 新建 `CallReceiver.java`

```java
public class CallReceiver extends BroadcastReceiver {
    private static String sPendingNumber;

    @Override
    public void onReceive(Context context, Intent intent) {
        String action = intent.getAction();
        // 来电：响铃时记录号码，挂断后创建便签
        if (PHONE_STATE_CHANGED.equals(action)) {
            if (RINGING.equals(state)) {
                sPendingNumber = intent.getStringExtra(EXTRA_INCOMING_NUMBER);
            } else if (IDLE.equals(state) && sPendingNumber != null) {
                createCallNote(context, sPendingNumber);
                sPendingNumber = null;
            }
        }
        // 去电：拨号时立即创建便签
        else if (NEW_OUTGOING_CALL.equals(action)) {
            String number = intent.getStringExtra(Intent.EXTRA_PHONE_NUMBER);
            createCallNote(context, number);
        }
    }

    private void createCallNote(Context context, String phoneNumber) {
        // 去重检查
        long noteId = DataUtils.getNoteIdByPhoneNumberAndCallDate(...);
        if (noteId > 0) return;

        // 使用 WorkingNote 标准流程创建
        WorkingNote note = WorkingNote.createEmptyNote(context,
                Notes.ID_CALL_RECORD_FOLDER,
                AppWidgetManager.INVALID_APPWIDGET_ID,
                Notes.TYPE_WIDGET_INVALIDE,
                ResourceParser.getDefaultBgId(context));
        note.convertToCallNote(phoneNumber, System.currentTimeMillis());
        note.saveNote();
    }
}
```

### 2. `AndroidManifest.xml` — 新增权限 + 注册 Receiver

**新增权限**：
```diff
+<uses-permission android:name="android.permission.READ_PHONE_STATE" />
+<uses-permission android:name="android.permission.PROCESS_OUTGOING_CALLS" />
```

| 权限 | 用途 |
|------|------|
| `READ_PHONE_STATE` | 读取来电号码（`EXTRA_INCOMING_NUMBER`） |
| `PROCESS_OUTGOING_CALLS` | 接收去电广播（`NEW_OUTGOING_CALL`） |

**注册 Receiver**（放在 `<application>` 最顶部，确保尽早初始化）：
```xml
<receiver
    android:name=".ui.CallReceiver"
    android:exported="true">
    <intent-filter>
        <action android:name="android.intent.action.PHONE_STATE" />
        <action android:name="android.intent.action.NEW_OUTGOING_CALL" />
    </intent-filter>
</receiver>
```

### 3. `NotesListActivity.java` — 运行时权限请求

`READ_PHONE_STATE` 是运行时权限（API 23+），App 首次启动时弹窗请求：

```diff
+import android.Manifest;
+import android.content.pm.PackageManager;
+import android.os.Build;
+
+private static final int REQUEST_CODE_READ_PHONE_STATE = 104;

  protected void onCreate(Bundle savedInstanceState) {
      ...
      setAppInfoFromRawRes();
+     requestPhonePermission();
  }

+private void requestPhonePermission() {
+    if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.M) {
+        if (checkSelfPermission(Manifest.permission.READ_PHONE_STATE)
+                != PackageManager.PERMISSION_GRANTED) {
+            requestPermissions(new String[]{
+                    Manifest.permission.READ_PHONE_STATE
+            }, REQUEST_CODE_READ_PHONE_STATE);
+        }
+    }
+}
```

**工作流程**：

```
来电:
  系统拨号器响铃 → PHONE_STATE=RINGING → CallReceiver 记录号码
  用户挂断      → PHONE_STATE=IDLE    → CallReceiver 创建便签

去电:
  用户拨号      → NEW_OUTGOING_CALL   → CallReceiver 立即创建便签
```

**与现有代码的兼容性**：

- 使用 `WorkingNote.createEmptyNote()` → `convertToCallNote()` → `saveNote()` 标准流程
- `convertToCallNote()` 设置 `PARENT_ID = ID_CALL_RECORD_FOLDER`，便签自动归入通话便签文件夹
- 去重逻辑复用 `DataUtils.getNoteIdByPhoneNumberAndCallDate()`
- 联系人姓名查询复用 `Contact.getContact()`
- 列表显示复用 `NoteItemData` / `NotesListItem` 的通话便签展示逻辑

**效果**：

- 模拟器（及任何标准 Android 拨号器）来电/去电后自动创建通话便签
- 通话便签文件夹自动出现在主列表（有便签时才显示）
- 便签中包含电话号码和通话时间，自动匹配联系人姓名
- 与 MIUI 原始通话便签功能一致，仅入口不同

> **注**：修改 15 在 API 36 上不生效，原因如下。修改 16 添加了 CallLog ContentObserver 后备方案解决。

---

## 修改 16：CallReceiver 增强日志 + CallLog ContentObserver 后备方案

**日期**：2026-05-31

**问题描述**：

修改 15 的 `CallReceiver`（监听 `PHONE_STATE` 广播）在模拟器拨号后没有触发，通话便签文件夹始终为空。

**根因分析**：

`PHONE_STATE` 广播在 API 34+（Android 14+）受到严格限制：
- 系统不再向所有注册了 `PHONE_STATE` intent-filter 的应用发送广播
- 仅默认电话应用和系统组件收到该广播
- 即使应用有 `READ_PHONE_STATE` 权限也不行

**方案**：保留 `CallReceiver`（在老设备上仍可能生效），新增 `CallLogObserver`——监听系统通话记录数据库 `CallLog.Calls` 的变化。拨号后通话记录必然写入数据库，这是系统级保证，不受广播限制影响。

**修改文件**：

| 文件 | 路径 |
|------|------|
| CallReceiver.java | `Notesmaster/app/src/main/java/net/micode/notes/ui/CallReceiver.java` |
| AndroidManifest.xml | `Notesmaster/app/src/main/AndroidManifest.xml` |
| NotesListActivity.java | `Notesmaster/app/src/main/java/net/micode/notes/ui/NotesListActivity.java` |

**修改内容**：

### 1. `CallReceiver.java` — 添加调试日志

- 所有关键分支加 `Log.w(TAG, ...)`（warning 级别，不用 debug 也可见）
- `createCallNote()` 改为 `static` 方法，可供 `NotesListActivity` 直接调用
- 每步都有日志输出，便于在 Logcat 中追踪

### 2. `AndroidManifest.xml` — 新增 READ_CALL_LOG 权限

```diff
+<uses-permission android:name="android.permission.READ_CALL_LOG" />
```

`READ_CALL_LOG` 允许读取系统通话记录数据库，是 `CallLogObserver` 的必需权限。

### 3. `NotesListActivity.java` — 核心变更

**新增字段**：
```java
private long mLastProcessedCallId;
private CallLogObserver mCallLogObserver;
```

**新增 `onCreate` 中注册**：
```java
registerCallLogObserver();
```

**新增 `onResume` 中补检**：回到前台时扫描一遍通话记录，处理后台新增的通话。

**新增 `onDestroy` 中注销**。

**新增 `CallLogObserver` 内部类**：
```java
private class CallLogObserver extends ContentObserver {
    @Override
    public void onChange(boolean selfChange) {
        checkCallLogForNewCalls();
    }
}
```

**新增 `checkCallLogForNewCalls()`**：
```java
private void checkCallLogForNewCalls() {
    // 权限检查
    // 查询 CallLog.Calls 中最新一条记录
    // 如果 callId > mLastProcessedCallId（新通话）→ 调用 CallReceiver.createCallNote()
    // 更新 mLastProcessedCallId 避免重复处理
}
```

**更新 `requestPhonePermission()`**：同时请求 `READ_PHONE_STATE` 和 `READ_CALL_LOG` 两颗权限。

**工作原理**：

```
用户拨号/接听 → 挂断
  → 系统写入 CallLog.Calls 数据库
    → CallLogObserver.onChange() 被触发
      → checkCallLogForNewCalls()
        → 查询最新 call log entry
        → ID 比对去重（mLastProcessedCallId）
        → CallReceiver.createCallNote(context, phoneNumber)
          → WorkingNote.createEmptyNote → convertToCallNote → saveNote ✅

回到主列表 → onResume()
  → checkCallLogForNewCalls() 再次扫描
    → 补检后台新增的通话 ✅
```

**双重保障**：

| 机制 | API 限制 | 触发方式 |
|------|----------|---------|
| `CallReceiver` (PHONE_STATE) | API 34+ 不发给普通应用 | 系统广播 |
| `CallLogObserver` (ContentObserver) | 无限制，所有 API 可用 | 数据库变更通知 |

**效果**：

- 模拟器拨号后挂断，`CallLogObserver` 检测到通话记录新增 → 自动创建通话便签
- App 从后台回到前台时自动扫描是否有新增通话记录
- 去重机制防止重复创建
- `CallReceiver` 保留作为老设备上的额外保障

---

## 修改 17：修复通话便签创建逻辑 + 文件夹点击崩溃

**日期**：2026-05-31

**问题描述**：

1. Toast 弹出但文件夹不出现 → `WorkingNote.isWorthSaving()` 因 `mContent` 为空返回 false，`saveNote()` 未执行写入
2. 文件夹出现后点击崩溃 → `Contact.getContact()` 查询联系人数据库需要 `READ_CONTACTS` 权限但从未运行时请求

**修改文件**：

| 文件 | 路径 |
|------|------|
| CallReceiver.java | `Notesmaster/app/src/main/java/net/micode/notes/ui/CallReceiver.java` |
| NotesListActivity.java | `Notesmaster/app/src/main/java/net/micode/notes/ui/NotesListActivity.java` |
| NoteItemData.java | `Notesmaster/app/src/main/java/net/micode/notes/ui/NoteItemData.java` |
| strings.xml (3 种语言) | `Notesmaster/app/src/main/res/values*/strings.xml` |

**修改内容**：

### 1. `CallReceiver.java` — `createCallNote()` 写入通话时间正文

```java
note.convertToCallNote(phoneNumber, callDate);

SimpleDateFormat sdf = new SimpleDateFormat("yyyy-MM-dd HH:mm", Locale.getDefault());
String timeStr = sdf.format(new Date(callDate));
note.setWorkingText(context.getString(R.string.note_created_from_call, timeStr));

note.saveNote();
```

`WorkingNote.isWorthSaving()` 因正文为空跳过了写入。加通话时间作为正文使保存生效。

### 2. `NotesListActivity.java` — 简化 `checkCallLogForNewCalls()`

- 改用 `CallLog.Calls.DATE` 时间戳去重（`SharedPreferences` 存储 `last_call_log_check`）
- 用 `TextUtils.isEmpty()` 处理空白号码，自动改为 `"private"`
- 扫描到新通话时可配置 Toast 提示

### 3. `NoteItemData.java` — 联系人查询 try-catch

```java
try {
    mName = Contact.getContact(context, mPhoneNumber);
} catch (Exception e) {
    mName = null;
}
```

### 4. `NotesListActivity` — 权限请求加 `READ_CONTACTS`

```java
requestPermissions(new String[]{
    Manifest.permission.READ_PHONE_STATE,
    Manifest.permission.READ_CALL_LOG,
    Manifest.permission.READ_CONTACTS
}, REQUEST_CODE_READ_PHONE_STATE);
```

### 5. 字符串资源

| Key | 中文值 | 英文值 |
|-----|--------|--------|
| `note_created_from_call` | `%1$s 通话记录` | `Call on %1$s` |

**效果**：

- 模拟器拨号后挂断 → 回到 APP → 通话便签文件夹自动出现
- 便签正文显示通话时间
- 联系人姓名正常显示（权限授予后）
- 点击通话便签文件夹不崩溃，点击单条便签不打开（原始设计如此）

---

## 修改 18：仿照通话记录模型实现短信便签功能

**日期**：2026-05-31

**问题描述**：

项目中已预留 `ID_SMS_RECORD_FOLDER = -4` 常量但未实现任何短信便签功能。需要仿照已有的通话记录（CallNote）模型，完整实现短信便签的数据模型、广播接收、UI 展示和编辑功能。

**修改文件**：

| 文件 | 路径 | 操作 |
|------|------|------|
| Notes.java | `Notesmaster/app/src/main/java/net/micode/notes/data/Notes.java` | 修改 |
| NotesDatabaseHelper.java | `Notesmaster/app/src/main/java/net/micode/notes/data/NotesDatabaseHelper.java` | 修改 |
| Note.java | `Notesmaster/app/src/main/java/net/micode/notes/model/Note.java` | 修改 |
| WorkingNote.java | `Notesmaster/app/src/main/java/net/micode/notes/model/WorkingNote.java` | 修改 |
| DataUtils.java | `Notesmaster/app/src/main/java/net/micode/notes/tool/DataUtils.java` | 修改 |
| SmsReceiver.java | `Notesmaster/app/src/main/java/net/micode/notes/ui/SmsReceiver.java` | **新建** |
| NoteItemData.java | `Notesmaster/app/src/main/java/net/micode/notes/ui/NoteItemData.java` | 修改 |
| NotesListItem.java | `Notesmaster/app/src/main/java/net/micode/notes/ui/NotesListItem.java` | 修改 |
| NotesListActivity.java | `Notesmaster/app/src/main/java/net/micode/notes/ui/NotesListActivity.java` | 修改 |
| NoteEditActivity.java | `Notesmaster/app/src/main/java/net/micode/notes/ui/NoteEditActivity.java` | 修改 |
| note_item.xml | `Notesmaster/app/src/main/res/layout/note_item.xml` | 修改 |
| sms_record_folder.xml | `Notesmaster/app/src/main/res/menu/sms_record_folder.xml` | **新建** |
| sms_note_edit.xml | `Notesmaster/app/src/main/res/menu/sms_note_edit.xml` | **新建** |
| strings.xml (3 种语言) | `Notesmaster/app/src/main/res/values*/strings.xml` | 修改 |
| AndroidManifest.xml | `Notesmaster/app/src/main/AndroidManifest.xml` | 修改 |

### 一、数据层

#### 1. `Notes.java` — 新增 `SmsNote` 数据模型类

```java
public static final class SmsNote implements DataColumns {
    public static final String SMS_DATE = DATA1;       // 短信日期 (long)
    public static final String PHONE_NUMBER = DATA3;   // 电话号码 (text)
    public static final String SMS_BODY = DATA4;       // 短信正文 (text)

    public static final String CONTENT_TYPE = "vnd.android.cursor.dir/sms_note";
    public static final String CONTENT_ITEM_TYPE = "vnd.android.cursor.item/sms_note";
    public static final Uri CONTENT_URI = Uri.parse("content://" + AUTHORITY + "/sms_note");
}
```

`DataConstants` 新增：
```java
public static final String SMS_NOTE = SmsNote.CONTENT_ITEM_TYPE;
```

#### 2. `NotesDatabaseHelper.java` — 数据库版本升级 + 短信文件夹

- 数据库版本 4 → **5**
- `createSystemFolder()` 中新增：`ID_SMS_RECORD_FOLDER (-4)` 系统文件夹插入
- 新增 `upgradeToV5()` 方法：使用 `insertWithOnConflict(CONFLICT_IGNORE)` 为已存在数据库创建短信文件夹
- `onUpgrade()` 新增 v4→v5 升级分支

### 二、模型层

#### 3. `Note.java` — 新增短信数据存储与持久化

内部类 `NoteData` 新增：
- `mSmsDataId` / `mSmsDataValues` — 短信数据 ID 和 ContentValues
- `setSmsDataId(long)` / `setSmsData(String, String)` — 设置方法

`pushIntoContentResolver()` 中新增短信数据的插入/更新逻辑（完全仿照通话数据流程）。

#### 4. `WorkingNote.java` — 新增 `convertToSmsNote()`

```java
public void convertToSmsNote(String phoneNumber, long smsDate, String smsBody) {
    mNote.setSmsData(SmsNote.SMS_DATE, String.valueOf(smsDate));
    mNote.setSmsData(SmsNote.PHONE_NUMBER, phoneNumber);
    mNote.setSmsData(SmsNote.SMS_BODY, smsBody);
    mNote.setNoteValue(NoteColumns.PARENT_ID, String.valueOf(Notes.ID_SMS_RECORD_FOLDER));
}
```

`loadNoteData()` 中新增 `DataConstants.SMS_NOTE` 类型识别。

### 三、工具层

#### 5. `DataUtils.java` — 新增短信查询方法

- `getSmsNumberByNoteId(ContentResolver, long)` — 根据便签 ID 查询短信电话号码
- `getNoteIdByPhoneNumberAndSmsDate(ContentResolver, String, long)` — 根据号码+日期查便签 ID（去重）

### 四、广播接收器

#### 6. `SmsReceiver.java`（新建）— 监听系统短信广播

```java
public class SmsReceiver extends BroadcastReceiver {
    @Override
    public void onReceive(Context context, Intent intent) {
        if ("android.provider.Telephony.SMS_RECEIVED".equals(action)) {
            // 解析 PDU，提取发件人、正文、时间戳
            // 调用 createSmsNote() 创建短信便签
        }
    }
}
```

- 监听 `SMS_RECEIVED` 广播，自动创建短信便签
- 自带去重逻辑（`getNoteIdByPhoneNumberAndSmsDate`）
- 便签正文格式：`"{时间} 短信记录\n{短信正文}"`

### 五、UI 层

#### 7. `NoteItemData.java` — 支持短信文件夹数据

构造函数中新增 `parentId == ID_SMS_RECORD_FOLDER` 分支：
- 通过 `DataUtils.getSmsNumberByNoteId()` 获取电话号码
- 通过 `Contact.getContact()` 查找联系人姓名（带 try-catch 保护）
- 新增 `isSmsRecord()` 方法

#### 8. `NotesListItem.java` — 短信列表项展示

`bind()` 方法新增两个分支：
- `data.getId() == ID_SMS_RECORD_FOLDER` → 显示"短信便签"文件夹
- `data.getParentId() == ID_SMS_RECORD_FOLDER` → 显示短信条目（联系人+正文预览）

新增 `mSmsBody` TextView 字段引用。

#### 9. `NotesListActivity.java` — 短信文件夹导航

- `ListEditState` 枚举新增 `SMS_RECORD_FOLDER`
- `ROOT_FOLDER_SELECTION` 新增 `ID_SMS_RECORD_FOLDER` 的 OR 条件（有便签时才显示）
- `openFolder()` / `onBackPressed()` / `onPrepareOptionsMenu()` 均支持短信文件夹
- `ModeCallback` 中短信文件夹的移动菜单也隐藏（与通话文件夹一致）

#### 10. `NoteEditActivity.java` — 短信便签编辑

- 新增 `INTENT_EXTRA_SMS_DATE` 和 `INTENT_EXTRA_SMS_BODY` 常量
- `initActivityState()` 中新增短信便签 Intent 解析（phoneNumber+smsDate+smsBody）
- 支持去重与新建两种模式
- `onPrepareOptionsMenu()` 加载 `sms_note_edit` 菜单

### 六、布局与菜单

#### 11. `note_item.xml` — 新增 tv_sms_body

```xml
<TextView
    android:id="@+id/tv_sms_body"
    android:singleLine="true"
    android:ellipsize="end"
    android:visibility="gone" />
```

#### 12. `sms_record_folder.xml`（新建）— 仿 call_record_folder，含返回/搜索

#### 13. `sms_note_edit.xml`（新建）— 仿 call_note_edit，含删除/分享/提醒等

### 七、字符串资源（3 种语言）

| Key | 英文 | 简体中文 | 繁体中文 |
|-----|------|----------|----------|
| `note_created_from_sms` | SMS from %1$s:\n%2$s | %1$s 短信记录\n%2$s | %1$s 簡訊記錄\n%2$s |
| `sms_record_folder_name` | SMS notes | 短信便签 | 簡訊便籤 |

### 八、AndroidManifest.xml

- **新增权限**：`RECEIVE_SMS`、`READ_SMS`
- **新增 SmsReceiver 注册**：监听 `android.provider.Telephony.SMS_RECEIVED`
- **NoteEditActivity intent-filter** 新增 `sms_note` mimeType（ACTION_VIEW 和 INSERT_OR_EDIT）

### 数据流

```
短信到达 → SmsReceiver.onReceive()
    │
    ├─ 解析 PDU → 发件人、正文、时间戳
    ├─ 去重检查 (getNoteIdByPhoneNumberAndSmsDate)
    ├─ createSmsNote() → WorkingNote → convertToSmsNote → saveNote
    └─ 数据库: Note(PARENT_ID=-4) + Data(MIME_TYPE=sms_note)

用户查看 → NotesListActivity → ROOT_FOLDER_SELECTION 含 ID_SMS_RECORD_FOLDER
    └─ 点击"短信便签"文件夹 → SMS_RECORD_FOLDER 状态 → 列表展示

用户编辑 → NoteEditActivity
    └─ sms_note_edit 菜单 (删除/分享/字体/清单/提醒/桌面)
```

**效果**：

- 收到短信时 `SmsReceiver` 自动创建短信便签，内容为发件人+时间+短信正文
- "短信便签"文件夹在根列表中出现（有记录时）
- 列表项显示联系人姓名和短信内容预览
- 支持完整的便签编辑功能（删除、分享、提醒、发送到桌面等）
- 三重语言支持（中文简体、繁体、英文）

---

## 修改 19：发送短信（去电短信）也会创建短信便签

**日期**：2026-05-31

**问题描述**：

修改 18 实现的 `SmsReceiver` 只监听了 `SMS_RECEIVED`（收短信），但用户发送（去电）的短信不会触发此广播。需要同时监听发送的短信，确保发件箱中的短信也会自动创建便签。

**方案**：

Android 没有 `SMS_SENT` 之类的标准系统广播可用于第三方应用检测发送短信。最可靠的方式是通过 `ContentObserver` 监听系统短信数据库 `content://sms/sent` 的变化，并在 `onResume()` 时进行补检。

**修改文件**：

| 文件 | 路径 | 操作 |
|------|------|------|
| NotesListActivity.java | `Notesmaster/app/src/main/java/net/micode/notes/ui/NotesListActivity.java` | 修改 |

**修改内容**：

### 1. 新增 import

```diff
+import android.database.ContentObserver;
+import android.net.Uri;
```

### 2. 新增字段 `mSmsSentObserver`

```java
private SmsSentObserver mSmsSentObserver;
```

### 3. `onCreate()` — 注册 ContentObserver

```java
mSmsSentObserver = new SmsSentObserver();
getContentResolver().registerContentObserver(
        Uri.parse("content://sms"), true, mSmsSentObserver);
```

监听 `content://sms` 整个短信数据库（`notifyForDescendants=true`），确保发送短信后立即触发。

### 4. 新增 `onDestroy()` — 注销 ContentObserver

```java
@Override
protected void onDestroy() {
    super.onDestroy();
    if (mSmsSentObserver != null) {
        getContentResolver().unregisterContentObserver(mSmsSentObserver);
    }
}
```

### 5. `onResume()` — 前台补检

```diff
 protected void onResume() {
     super.onResume();
     checkCallLogForNewCalls();
+    checkSentSmsForNewMessages();
     startAsyncNotesListQuery();
 }
```

### 6. 新增 `checkSentSmsForNewMessages()` 方法

```java
private void checkSentSmsForNewMessages() {
    // 权限检查: READ_SMS
    // 查询 content://sms/sent，按 date ASC 排序
    // 用 last_sms_check (SharedPreferences) 去重
    // 遍历结果 → SmsReceiver.createSmsNote()
    // 更新 last_sms_check 时间戳
}
```

与 `checkCallLogForNewCalls()` 使用完全相同的模式：
- `SharedPreferences` 存储上次检查时间戳
- 查询 `content://sms/sent` 中 `date > lastCheckedTime` 的记录
- 调用 `SmsReceiver.createSmsNote()` 创建短信便签（自带去重）
- Toast 提示创建数量

### 7. `requestPhonePermission()` — 新增 `READ_SMS` 权限请求

```diff
 requestPermissions(new String[]{
     Manifest.permission.READ_PHONE_STATE,
     Manifest.permission.READ_CALL_LOG,
     Manifest.permission.READ_CONTACTS,
+    Manifest.permission.READ_SMS
 }, REQUEST_CODE_READ_PHONE_STATE);
```

### 8. 新增 `SmsSentObserver` 内部类

```java
private class SmsSentObserver extends ContentObserver {
    public SmsSentObserver() {
        super(null);
    }

    @Override
    public void onChange(boolean selfChange) {
        Log.w(TAG, "SmsSentObserver: onChange triggered");
        checkSentSmsForNewMessages();
    }
}
```

### 双重保障机制

| 机制 | 触发方式 | 负责 |
|------|----------|------|
| `SmsReceiver` | `SMS_RECEIVED` 广播 | 收短信 |
| `SmsSentObserver` + `onResume()` | ContentObserver / 前台补检 | 发短信 |

**工作流程**：

```
用户发送短信
  → 系统写入 content://sms/sent 数据库
    → SmsSentObserver.onChange() 触发
      → checkSentSmsForNewMessages()
        → 查询 date > last_sms_check 的新记录
        → SmsReceiver.createSmsNote() 创建便签 ✅

App 从后台回到前台 → onResume()
  → checkSentSmsForNewMessages() 补检
    → 处理后台期间发送的所有短信 ✅
```

**效果**：

- 发送短信后，`ContentObserver` 实时检测并创建短信便签
- App 切回前台时自动补检后台期间的发送记录
- 时间戳去重，不会重复创建
- 与收短信便签使用相同的 `SmsReceiver.createSmsNote()` 方法，数据格式完全一致

---

## 修改 20：修复搜索功能（2 个根因 + 4 个次要问题）

**日期**：2026-06-13

**问题描述**：

项目内的搜索功能完全不可用，共 6 个问题：
1. 用户在搜索框输入并回车后，`ACTION_SEARCH` Intent 未被 `NoteEditActivity` 处理，导致 Activity 直接关闭，搜索对话框消失且无任何结果
2. `searchable.xml` 中 `searchSuggestAuthority="notes"` 与实际 ContentProvider 授权名 `"micode_notes"` 不匹配，导致输入时搜索建议从未出现
3. `sub_folder.xml` 缺少搜索菜单项，用户文件夹内无法触发搜索
4. `AndroidManifest.xml` 第 80 行 XML 格式错误（`</intent-filter >>`）
5. 缺少 "无搜索结果" 字符串资源
6. `search_result.png` 仅存在于 `drawable-hdpi`

**修改文件**：

| 文件 | 路径 |
|------|------|
| NoteEditActivity.java | `Notesmaster/app/src/main/java/net/micode/notes/ui/NoteEditActivity.java` |
| searchable.xml | `Notesmaster/app/src/main/res/xml/searchable.xml` |
| sub_folder.xml | `Notesmaster/app/src/main/res/menu/sub_folder.xml` |
| AndroidManifest.xml | `Notesmaster/app/src/main/AndroidManifest.xml` |
| strings.xml (3 种语言) | `Notesmaster/app/src/main/res/values*/strings.xml` |
| search_result.png (4 密度) | `Notesmaster/app/src/main/res/drawable-*/search_result.png` |

**修改内容**：

### 1. `NoteEditActivity.java` — 新增 `handleSearchIntent()` 处理 `ACTION_SEARCH`

**新增 import**：
```diff
+import android.database.Cursor;
```

**修改 `onCreate()`**：在 `setContentView` 之前检测 `ACTION_SEARCH`，有则调用 `handleSearchIntent()` 并 return。

**修改 `onNewIntent()`**：同样检测并代理到 `handleSearchIntent()`。

**新增 `handleSearchIntent()` 方法**：
```java
private void handleSearchIntent(Intent intent) {
    // 1. 从 SearchManager.QUERY 提取搜索词
    // 2. 查询 content://micode_notes/search?pattern=...
    // 3. 从 Cursor 读取 note ID 和 snippet
    // 4. 有结果 → AlertDialog 列表（点按打开笔记）
    // 5. 无结果 → Toast 提示 "No notes found" 并 finish()
}
```

搜索结果对话框使用 `AlertDialog.Builder.setItems()`，列表项显示去除 ✓/☐ 标记的笔记片段。点按结果以 `ACTION_VIEW` 打开 `NoteEditActivity`，搜索词通过 `getHighlightQueryResult()` 高亮显示。

### 2. `searchable.xml` — 修复搜索建议授权

```diff
-    android:searchSuggestAuthority="notes"
+    android:searchSuggestAuthority="micode_notes"
```

使 Android 搜索框架能正确定位到 `NotesProvider`（授权名 `micode_notes`），搜索建议恢复可显示。

### 3. `sub_folder.xml` — 添加搜索菜单项

```diff
     <item android:id="@+id/menu_new_note" ... />
+
+    <item
+        android:id="@+id/menu_search"
+        android:title="@string/menu_search"
+        android:showAsAction="ifRoom"/>
```

无需 Java 代码修改，`NotesListActivity.onOptionsItemSelected()` 已处理 `R.id.menu_search`。

### 4. `AndroidManifest.xml` — 修复 XML 格式

```diff
-            </intent-filter >>
+            </intent-filter>
```

### 5. 字符串资源（3 种语言）

| Key | 英文 | 简体中文 | 繁体中文 |
|-----|------|----------|----------|
| `search_no_results` | No notes found | 没有找到便签 | 沒有找到便籤 |

### 6. 图标资源

将 `search_result.png` 从 `drawable-hdpi` 复制到 `drawable-mdpi`、`drawable-xhdpi`、`drawable-xxhdpi`。

**用户交互流程**：

```
用户点击搜索图标
  → 搜索对话框出现 (hint: "搜索便签")
    → 用户输入文字
      → 系统查询 content://micode_notes/search_suggest_query/...
        → NotesProvider 执行 NOTES_SNIPPET_SEARCH_QUERY
          → 匹配的笔记标题作为建议出现 ✅
    → 用户点按建议
      → ACTION_VIEW + noteId → NoteEditActivity 打开笔记 ✅
    → 用户按回车
      → ACTION_SEARCH → NoteEditActivity.handleSearchIntent()
        → 查询 content://micode_notes/search?pattern=...
          → 有结果 → AlertDialog 列表 → 点按打开笔记 ✅
          → 无结果 → Toast "没有找到便签" ✅
```

**搜索覆盖范围**：

搜索 SQL 查询 `note.snippet LIKE '%query%'`，而 `snippet` 列通过数据库触发器 (`DATA_UPDATE_NOTE_CONTENT_ON_INSERT_TRIGGER` 等) 与 `data.content` 保持同步，因此搜索可覆盖全部笔记正文内容，包括文本笔记、通话便签和短信便签。

**效果**：

- 搜索建议随输入实时出现
- 点按建议直接打开笔记，搜索词高亮
- 回车提交显示结果列表对话框
- 无结果时友好提示
- 所有文件夹视图（根列表、子文件夹、通话记录、短信记录）均可触发搜索
- 三种语言均已完成本地化

---

## 修改 21：升级闹钟时间选择器为日历视图

**日期**：2026-06-13

**问题描述**：

原有闹钟的日期选择使用 `NumberPicker` 滚轮，仅显示 7 天滚动窗口（"MM.dd EEEE" 格式，如 "06.13 Monday"），用户无法直观地通过日历选择任意日期。

**方案**：

新建 `DateTimePickerCalendar` 组件，使用 Android 内置 `DatePicker`（日历网格模式）+ `TimePicker`（滚轮模式）替换原有的 4 个 `NumberPicker` 滚轮。`DateTimePickerDialog` 仅需改动类型引用，所有上层代码（`NoteEditActivity`、`WorkingNote`、`AlarmManager` 调度）无需任何修改。

**修改文件**：

| 文件 | 路径 | 操作 |
|------|------|------|
| datetime_picker_new.xml | `Notesmaster/app/src/main/res/layout/datetime_picker_new.xml` | **新建** |
| DateTimePickerCalendar.java | `Notesmaster/app/src/main/java/net/micode/notes/ui/DateTimePickerCalendar.java` | **新建** |
| DateTimePickerDialog.java | `Notesmaster/app/src/main/java/net/micode/notes/ui/DateTimePickerDialog.java` | 修改 |

**修改内容**：

### 1. `datetime_picker_new.xml`（新建）

垂直 `LinearLayout` 布局，包含：
- `DatePicker`（`calendarViewShown="true"`, `datePickerMode="calendar"`）— 日历网格视图
- `TimePicker`（`timePickerMode="spinner"`）— 时钟滚轮

### 2. `DateTimePickerCalendar.java`（新建）

`FrameLayout` 子类，提供与原 `DateTimePicker` 兼容的公开 API：
- `getCurrentDateInTimeMillis()` → `long`
- `setCurrentDate(long)` / `setCurrentDate(int year, int month, int day, int hour, int minute)`
- `setOnDateTimeChangedListener(OnDateTimeChangedListener)`
- `set24HourView(boolean)`
- 日期/时间各字段独立的 getter/setter

内部使用 `Calendar mDate` 维护完整的日期+时间，`DatePicker` 通过 `init()` 设置并监听，`TimePicker` 通过 `setOnTimeChangedListener()` 监听。

### 3. `DateTimePickerDialog.java`

```diff
-    private DateTimePicker mDateTimePicker;
+    private DateTimePickerCalendar mDateTimePicker;

-    mDateTimePicker = new DateTimePicker(context);
+    mDateTimePicker = new DateTimePickerCalendar(context, date);
```

日期通过构造函数直接传入。其余逻辑（OK/Cancel 按钮、标题更新、`OnDateTimeSetListener` 回调）完全不变。

**无需修改**：`NoteEditActivity.java`、`WorkingNote.java`、`AlarmReceiver.java`、`AlarmAlertActivity.java`、`AlarmInitReceiver.java`

**效果**：

- 点击"提醒我"后弹出日历视图，可翻阅任意月份选择日期
- 下方时间滚轮独立选择小时和分钟（AM/PM 自动适配系统 12/24 小时制）
- 标题实时显示当前选择的完整日期+时间
- 设置后的闹钟调度、通知、锁屏弹窗、重启恢复逻辑与原行为完全一致

**测试结果**：
- 构建成功 ✅
- 日历视图可翻阅月份选择任意日期 ✅
- 时间滚轮正常，AM/PM 适配正确 ✅
- 确认后闹钟正常设置（通过已有 AlarmManager 逻辑） ✅

---

## 修改 22：同步功能改造 — Google Tasks → GitHub 私有仓库

**日期**：2026-06-13

**问题描述**：

项目现有同步功能基于 Google Tasks API（`gtask/` 包），依赖 Google 账号登录：
- 同步按钮在 `note_list.xml` 被硬编码 `android:visible="false"`
- Google 服务在国内无法访问
- 代码中有 TODO 注释 "后续迁移至阿里云同步服务"

**方案**：使用 GitHub 私有仓库 `xiaomibianqian` 作为同步后端，通过 GitHub Contents API 进行笔记的 CRUD 操作。用户通过 GitHub Personal Access Token 登录，数据以 JSON 文件形式存储在私人仓库中。

**方案优势**：

| 维度 | GitHub 私有仓库 | Google Tasks（旧） |
|------|----------------|-------------------|
| 国内访问 | ✅ 可用 | ❌ 被墙 |
| API 协议 | JSON REST | 私有 JSON RPC |
| 免费额度 | 无限私有仓库 | 免费 |
| 数据版本 | Git 天然回滚 | 无 |

**仓库目录结构**（仓库名：`xiaomibianqian`）：

```
xiaomibianqian/
├── [MIUI_Notes]Default/          ← 根文件夹（笔记列表）
│   ├── {note_gid}.json
│   └── ...
├── [MIUI_Notes]Call_Note/        ← 通话记录文件夹
├── [MIUI_Notes]SMS_Record/       ← 短信记录文件夹
├── [MIUI_Notes]METADATA/         ← 隐藏元数据列表
└── {用户自定义文件夹}/
```

**GitHub Contents API 映射**：

| 同步操作 | GitHub API | HTTP 方法 |
|---------|-----------|----------|
| 列出文件夹 | `GET /repos/{owner}/{repo}/contents/{path}` | GET |
| 读取文件 | `GET /repos/{owner}/{repo}/contents/{path}` | GET |
| 创建/更新 | `PUT /repos/{owner}/{repo}/contents/{path}` | PUT + base64 content + sha |
| 删除文件 | `DELETE /repos/{owner}/{repo}/contents/{path}` | DELETE + sha |
| 移动文件 | GET → PUT → DELETE（三步模拟） | - |

**认证方案**：Personal Access Token（PAT），用户创建带 `repo` scope 的 token，粘贴到 App 设置。后续可升级为 OAuth。

**修改文件**：

| 文件 | 路径 | 操作 |
|------|------|------|
| SyncClient.java | `Notesmaster/app/src/main/java/net/micode/notes/sync/SyncClient.java` | **新建** |
| SyncException.java | `Notesmaster/app/src/main/java/net/micode/notes/sync/SyncException.java` | **新建** |
| SyncClientFactory.java | `Notesmaster/app/src/main/java/net/micode/notes/sync/SyncClientFactory.java` | **新建** |
| GitHubSyncClient.java | `Notesmaster/app/src/main/java/net/micode/notes/sync/github/GitHubSyncClient.java` | **新建** |
| GitHubAuthActivity.java | `Notesmaster/app/src/main/java/net/micode/notes/sync/github/GitHubAuthActivity.java` | **新建** |
| GTaskManager.java | `Notesmaster/app/src/main/java/net/micode/notes/gtask/remote/GTaskManager.java` | 修改 |
| note_list.xml | `Notesmaster/app/src/main/res/menu/note_list.xml` | 修改 |
| NotesListActivity.java | `Notesmaster/app/src/main/java/net/micode/notes/ui/NotesListActivity.java` | 修改 |
| NoteEditActivity.java | `Notesmaster/app/src/main/java/net/micode/notes/ui/NoteEditActivity.java` | 修改 |
| NotesPreferenceActivity.java | `Notesmaster/app/src/main/java/net/micode/notes/ui/NotesPreferenceActivity.java` | 修改 |
| preferences.xml | `Notesmaster/app/src/main/res/xml/preferences.xml` | 修改 |
| strings.xml (3 种语言) | `Notesmaster/app/src/main/res/values*/strings.xml` | 修改 |
| build.gradle.kts | `Notesmaster/app/build.gradle.kts` | 修改 |
| AndroidManifest.xml | `Notesmaster/app/src/main/AndroidManifest.xml` | 修改 |

**架构设计**：

```
UI 层（小改动）
  NotesListActivity → 恢复 menu_sync 按钮
  NotesPreferenceActivity → GitHub 登录/Token 配置
  GTaskSyncService / GTaskASyncTask（不改）
        │
同步引擎层（小改动）
  GTaskManager → 改为依赖 SyncClient 接口（而非 GTaskClient）
  Node/Task/TaskList/MetaData/SqlNote/SqlData（全部不改）
        │
新增抽象层
  SyncClient 接口 → 后端无关的 CRUD 契约
  SyncException → 统一异常
  SyncClientFactory → 工厂
        │
GitHub 实现（新增）
  GitHubSyncClient → OkHttp + GitHub Contents API
  GitHubAuthActivity → PAT 输入 / OAuth 登录
```

**关键实现细节**：

1. **sha 乐观锁冲突检测**：GitHub Contents API 要求更新文件时提供 `sha` 参数。两台设备同时修改同一笔记时，第二台设备的 PUT 返回 409 Conflict → 触发 `SYNC_ACTION_UPDATE_CONFLICT`

2. **空文件夹处理**：GitHub 不支持空目录，用 `.gitkeep` 占位文件，客户端列出文件时自动过滤

3. **API 限速**：认证用户 5000 次/小时，笔记同步绰绰有余。如需要可加条件请求 `If-None-Match: <etag>` 节省配额

4. **OkHttp 替换 Apache HttpClient**：原来的 `GTaskClient` 使用 Apache HttpClient（已废弃），新 `GitHubSyncClient` 使用 OkHttp 4.12

**保留不变的文件**：

- `gtask/data/` 全部（Node, Task, TaskList, MetaData, SqlNote, SqlData）— 数据映射层完全复用
- `gtask/remote/GTaskClient.java` — 保留作为参考
- `gtask/exception/` — 保留，新异常在 sync 包
- `tool/GTaskStringUtils.java` — 常量仍被数据层使用
- `GTaskSyncService.java` / `GTaskASyncTask.java` — 后端无关，不改

**用户同步流程**：

```
1. GitHub → Settings → Developer settings → PAT
   → 创建 token（勾选 repo scope） → 复制

2. App 设置 → 同步 → 粘贴 token → 验证连接
   → 显示 "已连接为 @username"

3. 点击同步按钮
   → GTaskSyncService → GTaskManager.sync()
     → GitHubSyncClient 列出远程文件
     → 与本地数据库对比（复用现有 GTaskManager 逻辑）
     → 上传新增 / 下载更新 / 删除
     → 通知同步结果
```

**验证计划**：

1. GitHub 创建私人仓库 `xiaomibianqian`，生成 PAT
2. App 设置中输入 PAT，验证连接成功
3. 同步 → 笔记上传到 GitHub 仓库
4. 另一设备/清除数据后配置同一仓库 → 笔记下载还原
5. 验证文件夹结构、元数据（颜色、提醒）完整
6. 两台设备同时修改同一笔记 → 验证 409 冲突提示
7. 删除笔记后同步 → 验证远程文件被删除

---

## 修改 22 补充：引导页中文化 + 设置入口完善 + 崩溃修复

**日期**：2026-06-13

**问题描述**：

1. GitHub 配置引导页在英文系统上显示英文，国内用户体验差
2. 主列表点击"设置"菜单只弹出一个背景色复选框对话框，无法进入真正的设置页配置 GitHub Token
3. 点击同步按钮后，`GTaskSyncService.onStartCommand()` 因 `intent` 为 null 而 NPE 崩溃

**修改文件**：

| 文件 | 路径 | 操作 |
|------|------|------|
| GitHubAuthActivity.java | `Notesmaster/app/src/main/java/net/micode/notes/sync/github/GitHubAuthActivity.java` | 修改 |
| github_auth.xml | `Notesmaster/app/src/main/res/layout/github_auth.xml` | 重写 |
| NotesListActivity.java | `Notesmaster/app/src/main/java/net/micode/notes/ui/NotesListActivity.java` | 修改 |
| NotesPreferenceActivity.java | `Notesmaster/app/src/main/java/net/micode/notes/ui/NotesPreferenceActivity.java` | 修改 |
| GTaskSyncService.java | `Notesmaster/app/src/main/java/net/micode/notes/gtask/remote/GTaskSyncService.java` | 修改 |
| strings.xml (3 种语言) | `Notesmaster/app/src/main/res/values*/strings.xml` | 修改 |

**修改内容**：

### 1. 引导页中文化

- `GitHubAuthActivity.onCreate()` 开头调用 `forceChineseLocale()`，强制使用简体中文语言环境
- `github_auth.xml` 重写为 ScrollView + 分步引导布局，包含：
  - 功能简介文字
  - 📋 4 步获取 Token 教程
  - Token 输入框（hint: ghp_xxxxxxxxxxxxxxxxxxxx）
  - 测试连接 + 保存并完成 按钮
  - 底部提示（Token 只显示一次、需先创建仓库）
- 三种语言全面更新：简体中文、繁体中文、英文，新增 `preferences_github_intro`、`preferences_github_step_title`、`preferences_github_step1~4`、`preferences_github_token_hint`、`preferences_github_tip` 等字符串

### 2. 设置入口完善

- **`NotesListActivity.startPreferenceActivity()`**：从弹出背景色复选框对话框改为启动真正的 `NotesPreferenceActivity`
- **`NotesPreferenceActivity.loadAccountPreference()`**：未配置 GitHub 时，点击同步账号直接打开 `GitHubAuthActivity` 引导页（而非旧 Google 账号对话框）
- **`NotesListActivity` 同步按钮**：未配置时直接打开 `GitHubAuthActivity`（而非旧设置弹窗）

所有路径均可配置 Token：
- 主列表 → 同步按钮（未配置） → GitHubAuthActivity
- 主列表 → 设置 → 同步账号（未配置） → GitHubAuthActivity
- 主列表 → 设置 → 同步账号（已配置） → 管理对话框 → 更换账号 → GitHubAuthActivity

### 3. GTaskSyncService NPE 修复

`onStartCommand()` 在 `START_STICKY` 模式下系统可能传入 null intent，添加空判断：

```java
if (intent == null) {
    return START_STICKY;
}
```

### 4. 清理无用 import

- `NotesListActivity.java`：移除 `CheckBox` import（原设置弹窗已移除），保留 `PreferenceManager`（仍被通话/短信检测逻辑使用）

**效果**：

- 引导页无论系统语言始终显示中文，分步教程清晰易懂
- 用户可从主列表→设置随时访问 GitHub Token 配置
- 同步按钮点击不再崩溃
- 设置菜单打开真正的设置页，而非简陋的复选框弹窗

---

## 修改 23：修复 GitHub 同步 4 个运行时崩溃 + API 认证问题

**日期**：2026-06-13

**问题描述**：

修改 22 实现 GitHub 同步后在真实设备上测试发现 4 个严重问题：
1. GitHub Token 测试连接一直报"无效"——缺少 `User-Agent` 请求头
2. 打开设置页直接崩溃——`registerReceiver()` API 34+ 未指定 `RECEIVER_NOT_EXPORTED`
3. 点击同步按钮崩溃——`GTaskASyncTask` 在 `GTaskManager.mContext` 为 null 时调用 `getSyncAccount()`
4. `GTaskSyncService.onStartCommand()` 在系统重启 Service 时传入 null intent → NPE

**修改文件**：

| 文件 | 路径 | 操作 |
|------|------|------|
| GitHubSyncClient.java | `Notesmaster/app/src/main/java/net/micode/notes/sync/github/GitHubSyncClient.java` | 修改 |
| NotesPreferenceActivity.java | `Notesmaster/app/src/main/java/net/micode/notes/ui/NotesPreferenceActivity.java` | 修改 |
| GTaskASyncTask.java | `Notesmaster/app/src/main/java/net/micode/notes/gtask/remote/GTaskASyncTask.java` | 修改 |
| GTaskManager.java | `Notesmaster/app/src/main/java/net/micode/notes/gtask/remote/GTaskManager.java` | 修改 |
| GTaskSyncService.java | `Notesmaster/app/src/main/java/net/micode/notes/gtask/remote/GTaskSyncService.java` | 修改 |

**修改内容**：

### 1. GitHub API 请求缺 `User-Agent` 头（Token 验证失败）

**根因**：GitHub API 对所有请求强制要求 `User-Agent` 头，无此头返回 403 Forbidden。

**修复**：提取 `buildRequest()` 公共方法，统一添加以下头：
- `Authorization: Bearer <token>`（从 `token <token>` 改为现代标准）
- `User-Agent: Notesmaster/1.0`（GitHub API 必需）
- `Accept: application/vnd.github.v3+json`

```java
private Request.Builder buildRequest(String path) {
    return new Request.Builder()
            .url(GITHUB_API + path)
            .header("Authorization", "Bearer " + mToken)
            .header("Accept", "application/vnd.github.v3+json")
            .header("User-Agent", "Notesmaster/1.0");
}
```

### 2. 设置页 `registerReceiver()` 崩溃（API 34+）

**根因**：`NotesPreferenceActivity.onCreate()` 中注册 `GTaskReceiver` 时未指定 `RECEIVER_EXPORTED`/`RECEIVER_NOT_EXPORTED`。targetSdk 36 要求必须显式声明。

**修复**：
```java
if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.TIRAMISU) {
    registerReceiver(mReceiver, filter, Context.RECEIVER_NOT_EXPORTED);
} else {
    registerReceiver(mReceiver, filter);
}
```

### 3. 同步按钮 NPE（`getSyncAccount()` 在 `mContext` 为 null 时调用）

**根因**：`GTaskASyncTask.doInBackground()` 在调用 `mTaskManager.sync()` **之前**先调了 `GTaskManager.getInstance().getSyncAccount()` 来展示登录进度文字。但 `GTaskManager.mContext` 只会在 `sync()` 方法内被赋值，此时仍为 null → `SyncClientFactory.getSyncClient(null)` → `SharedPreferences` 操作抛 NPE。

**修复**：
- `GTaskASyncTask.doInBackground()`：直接用 `SyncClientFactory.getSyncClient(mContext)` 获取 GitHub 用户名（用 Service 自身的 context）
- `GTaskManager.getSyncAccount()`：加 `mContext` 空判断防护

```java
// GTaskASyncTask: 先用 Service context 直接获取账号名
net.micode.notes.sync.SyncClient client =
        net.micode.notes.sync.SyncClientFactory.getSyncClient(mContext);
if (client != null) {
    accountName = client.getAccountIdentifier();
}

// GTaskManager: 加空判断
public String getSyncAccount() {
    if (mContext == null) return "";
    ...
}
```

### 4. `GTaskSyncService` null intent 崩溃

**根因**：`onStartCommand()` 使用 `START_STICKY`，系统在低内存杀进程后重启 Service 时传入 null intent，`intent.getExtras()` 抛 NPE。

**修复**：方法开头加 null 判断：
```java
if (intent == null) {
    return START_STICKY;
}
```

**效果**：

- GitHub Token 可以正常验证和连接
- 设置页在 Android 14+ 设备上不再崩溃
- 同步按钮点击后正常启动同步流程
- Service 被系统重启后不会因 null intent 崩溃

---

## 修改 24：修复同步闪退 — 前台服务 + 线程安全 + 实例泄漏

**日期**：2026-06-13

**问题描述**：

用户报告同步时应用闪退。经过对全部同步代码的彻底分析，定位到 1 个主因和 3 个次因。

**主因分析**：

`GTaskSyncService` 继承自 `Service`，从未调用 `startForeground()`。在 Android 8.0+（API 26+）系统上，后台 Service 若在启动后约 5 秒内未提升为前台服务，系统会直接将其杀死。同步涉及网络请求（GitHub API），HTTP 通信耗时远超 5 秒 → Service 在同步过程中被杀 → 进程终止 → 应用闪退。

**次因**：

1. `GTaskASyncTask.showNotification()` 在每次进度更新时重复创建通知渠道（`createNotificationChannel`），且渠道创建依赖于进度回调触发，刚启动 sync 时渠道可能尚不存在
2. `SyncClientFactory.getSyncClient()` 静态单例字段 `sInstance` 无同步保护，多线程并发时可能创建多个实例
3. 切换同步提供商时 `GitHubSyncClient.sInstance` 不会被清理，导致旧实例（含缓存数据）在新配置下继续使用

**修改文件**：

| 文件 | 路径 | 操作 |
|------|------|------|
| GTaskSyncService.java | `Notesmaster/app/src/main/java/net/micode/notes/gtask/remote/GTaskSyncService.java` | 修改 |
| GTaskASyncTask.java | `Notesmaster/app/src/main/java/net/micode/notes/gtask/remote/GTaskASyncTask.java` | 修改 |
| SyncClientFactory.java | `Notesmaster/app/src/main/java/net/micode/notes/sync/SyncClientFactory.java` | 修改 |
| GitHubSyncClient.java | `Notesmaster/app/src/main/java/net/micode/notes/sync/github/GitHubSyncClient.java` | 修改 |

**修改内容**：

### 1. `GTaskSyncService.java` — 前台服务化（核心修复）

**新增 import**：`Notification`、`NotificationChannel`、`NotificationManager`、`PendingIntent`、`Build`

**新增常量**：
```java
static final String SYNC_CHANNEL_ID = "sync_channel";
private static final int SYNC_NOTIFICATION_ID = 5234235;
```

**`onCreate()` 增强**：初始化 `NotificationManager` 并调用 `createSyncChannel()` 预先创建通知渠道，确保在任何通知（包括前台服务通知）发送前渠道已存在。

```java
@Override
public void onCreate() {
    super.onCreate();
    mSyncTask = null;
    mNotifiManager = (NotificationManager) getSystemService(Context.NOTIFICATION_SERVICE);
    createSyncChannel();
}
```

**`startSync()` 增强**：在 `AsyncTask.execute()` 之前调用 `startForeground()`，将服务提升为前台服务：

```java
startForeground(SYNC_NOTIFICATION_ID, buildForegroundNotification(
        getString(R.string.sync_progress_login, "")));
mSyncTask.execute();
```

**`onComplete()` 回调增强**：同步完成后撤销前台状态：

```java
stopForeground(true);  // remove foreground notification
stopSelf();
```

**新增 `createSyncChannel()` 方法**：创建 `NotificationChannel`（`IMPORTANCE_LOW`），渠道描述指向同步初始化字符串。

**新增 `buildForegroundNotification()` 方法**：构建前台服务通知，Oreo+ 使用 channel-aware Builder，pre-Oreo 使用旧构造器，兼容所有 API 版本。

### 2. `GTaskASyncTask.java` — 渠道复用 + 清理

**移除** `NotificationChannel` import（不再自行创建渠道）。

**`showNotification()` 重构**：
- 渠道 ID 改为 `GTaskSyncService.SYNC_CHANNEL_ID`（复用 Service 预创建的渠道）
- 移除方法内 `createNotificationChannel()` 调用
- 兼容 pre-Oreo 构造器

**`GTASK_SYNC_NOTIFICATION_ID` 改为 `final`**，与 Service 的 `SYNC_NOTIFICATION_ID` 值保持一致（`5234235`），确保前台服务通知与进度通知共用一个槽位，不会出现两个通知。

### 3. `SyncClientFactory.java` — 线程安全

**`getSyncClient()` 加 `synchronized`**：防止多线程并发调用时因非原子性 `check-then-act` 导致创建多个实例。

**`setSyncProvider()` 加 `synchronized`**：
```java
public static synchronized void setSyncProvider(Context context, String provider) {
    ...
    sInstance = null;
    GitHubSyncClient.resetInstance();  // 新增：级联清理
}
```

切换提供商（或清除配置）时，除了清理本类缓存，还会调用 `GitHubSyncClient.resetInstance()` 清理下级单例，确保下次 `getSyncClient()` 创建全新的、无残留状态的实例。

### 4. `GitHubSyncClient.java` — 新增 `resetInstance()`

```java
/**
 * Reset the cached singleton so the next getInstance creates a new one.
 * Called by SyncClientFactory.setSyncProvider when the user switches providers.
 */
public static synchronized void resetInstance() {
    sInstance = null;
}
```

方法可见性为 `public`（因为 `SyncClientFactory` 在不同包 `net.micode.notes.sync`，而 `GitHubSyncClient` 在 `net.micode.notes.sync.github`）。

**前台服务机制对比**：

```
修改前:
  startService()
  → AsyncTask.execute()
  → 网络请求 5s+
    → 系统检测到后台 Service 未 startForeground()
    → KILL 进程 ❌ 闪退

修改后:
  startService()
  → startForeground(id, notification) ← 系统注册前台服务
  → AsyncTask.execute()
  → 网络请求（安全进行...）
  → onComplete() → stopForeground(true) → stopSelf() ✅ 正常结束
```

**效果**：

- 同步 Service 提升为前台服务，系统不再因超时杀死进程，同步期间稳定运行
- 同步进度通知与前台上通知共用同一槽位，用户体验一致
- 通知渠道在 Service 启动时一次性创建，所有后续进度更新复用
- `SyncClientFactory` 线程安全，多线程环境正确
- 切换同步提供商时缓存实例正确清理，不会残留旧状态
- pre-Oreo (API < 26) 设备兼容，使用旧 Notification 构造器
- 编译通过 (`BUILD SUCCESSFUL`)，APK 正常生成

---

## 修改 24 补充：修复前台服务权限缺失（FOREGROUND_SERVICE + DATA_SYNC）

**日期**：2026-06-13

**问题描述**：

修改 24 将 `GTaskSyncService` 改造为前台服务后，在真实设备上点击同步按钮立刻崩溃：

```
java.lang.SecurityException: Permission Denial: startForeground from pid=30611, uid=10286
requires android.permission.FOREGROUND_SERVICE
```

**根因**：

Android 9.0（API 28）引入了 `FOREGROUND_SERVICE` 普通权限，`startForeground()` 必须在 `AndroidManifest.xml` 中声明该权限，否则系统拒绝调用。

此外，Android 14（API 34）进一步要求：
1. 声明具体的前台服务类型权限（如 `FOREGROUND_SERVICE_DATA_SYNC`）
2. 在 `<service>` 标签中指定 `android:foregroundServiceType="dataSync"`
3. 调用 `startForeground()` 时传入类型参数

我们的 targetSdk 为 36，三个条件缺一不可。

**修改文件**：

| 文件 | 路径 | 操作 |
|------|------|------|
| AndroidManifest.xml | `Notesmaster/app/src/main/AndroidManifest.xml` | 修改 |
| GTaskSyncService.java | `Notesmaster/app/src/main/java/net/micode/notes/gtask/remote/GTaskSyncService.java` | 修改 |

**修改内容**：

### 1. `AndroidManifest.xml` — 新增 2 个权限 + 服务类型声明

```diff
+<uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
+<uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
```

| 权限 | 适用 API | 用途 |
|------|----------|------|
| `FOREGROUND_SERVICE` | API 28+ | 允许使用 `startForeground()` |
| `FOREGROUND_SERVICE_DATA_SYNC` | API 34+ | 允许声明 `dataSync` 类型的前台服务 |

服务声明添加 type：
```diff
 <service
     android:name="net.micode.notes.gtask.remote.GTaskSyncService"
+    android:foregroundServiceType="dataSync"
     android:exported="false" >
 </service>
```

`dataSync` 是最贴合同步操作的前台服务类型（其他类型如 `camera`、`location`、`mediaPlayback` 等均与笔记同步无关）。

### 2. `GTaskSyncService.java` — API 34+ 类型参数

`startSync()` 中 `startForeground()` 调用改为版本分支：

```java
if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.UPSIDE_DOWN_CAKE) {
    startForeground(SYNC_NOTIFICATION_ID, notification,
            android.content.pm.ServiceInfo.FOREGROUND_SERVICE_TYPE_DATA_SYNC);
} else {
    startForeground(SYNC_NOTIFICATION_ID, notification);
}
```

**API 层级对照**：

| API 版本 | 所需权限 | startForeground 签名 |
|----------|----------|---------------------|
| < 28 | 无 | `(int, Notification)` |
| 28–33 | `FOREGROUND_SERVICE` | `(int, Notification)` |
| 34+ | `FOREGROUND_SERVICE` + `FOREGROUND_SERVICE_DATA_SYNC` | `(int, Notification, int)` |

**效果**：

- 同步按钮点击后 Service 正常启动为前台服务，不再抛 `SecurityException`
- 状态栏显示 "Notes" 同步通知（渠道: 同步），持续到同步完成
- API 28+ 设备正常同步
- API 34+ (Android 14/15) 设备正常同步，类型安全

---

## 修改 24 补充 2：修复 MetaData.getLocalJSONFromContent() IllegalAccessError + NPE 防护

**日期**：2026-06-13

**问题描述**：

修复前台服务权限后，用户再次测试同步。点击同步按钮后，在显示"正在同步本地便签到远程服务器..."后仍闪退。

**根因定位**：

`MetaData` 继承自 `Task`，但故意覆盖了两个方法抛 `IllegalAccessError`（因为原 Google Tasks API 注释明确写了 "should not be called"）：

```java
// MetaData.java
@Override
public JSONObject getLocalJSONFromContent() {
    throw new IllegalAccessError("MetaData:getLocalJSONFromContent should not be called");
}

@Override
public void setContentByLocalJSON(JSONObject js) {
    throw new IllegalAccessError("MetaData:setContentByLocalJSON should not be called");
}
```

新的 `GitHubSyncClient.updateItem()` / `createItem()` 对所有 Node（包括 MetaData）调用 `item.getLocalJSONFromContent()` → **💥 `IllegalAccessError`**

**关键：`IllegalAccessError` 是 `Error` 而非 `Exception`。** 原 `GTaskASyncTask.doInBackground()` 只有 `catch (Exception e)`，抓不住 Error，导致应用直接闪退（Uncaught Error → App crash）。

**调用链**：
```
GTaskManager.updateRemoteMeta()
  → syncClient.updateItem(metaData.getGid(), metaData)
    → GitHubSyncClient.updateItem(gid, item)  // item is MetaData
      → item.getLocalJSONFromContent()         // 💥 IllegalAccessError
        → AsyncTask 未捕获 Error → App 崩溃
```

同时发现两个防御性 NPE 漏洞：
- `Task.setContentByLocalJSON(null)` — 检查了 null 但忘记 `return`，继续执行 `js.getJSONObject()` → NPE
- `TaskList.setContentByLocalJSON(null)` — 同上

**修改文件**：

| 文件 | 路径 | 操作 |
|------|------|------|
| GitHubSyncClient.java | `Notesmaster/app/src/main/java/net/micode/notes/sync/github/GitHubSyncClient.java` | 修改 |
| GTaskASyncTask.java | `Notesmaster/app/src/main/java/net/micode/notes/gtask/remote/GTaskASyncTask.java` | 修改 |
| Task.java | `Notesmaster/app/src/main/java/net/micode/notes/gtask/data/Task.java` | 修改 |
| TaskList.java | `Notesmaster/app/src/main/java/net/micode/notes/gtask/data/TaskList.java` | 修改 |
| GTaskManager.java | `Notesmaster/app/src/main/java/net/micode/notes/gtask/remote/GTaskManager.java` | 修改 |

**修改内容**：

### 1. `GitHubSyncClient.java` — MetaData 特殊处理

`createItem()` 和 `updateItem()` 中添加 `instanceof MetaData` 分支。MetaData 通过 `setNotes()` 直接存储 JSON 字符串，无需通过 `getLocalJSONFromContent()`：

```java
// createItem / updateItem: 对 MetaData 特殊处理
if (item instanceof MetaData) {
    MetaData meta = (MetaData) item;
    String notes = meta.getNotes();
    if (notes == null) {
        Log.w(TAG, "Skipping MetaData with empty notes");
        return item.getGid();  // or return for updateItem
    }
    jsonStr = notes;
} else {
    // 正常 Task/TaskList 的序列化路径
    JSONObject localJson = item.getLocalJSONFromContent();
    ...
}
```

### 2. `GTaskASyncTask.java` — catch Throwable

```diff
-    } catch (Exception e) {
+    } catch (Throwable e) {
```
同时 `writeCrashLog` 参数从 `Exception` 改为 `Throwable`，内层 catch 也换为 `Throwable`。确保 `OutOfMemoryError`、`IllegalAccessError`、`NoClassDefFoundError` 等都不会导致闪退。

### 3. `Task.java` — NPE 修复

```diff
  public void setContentByLocalJSON(JSONObject js) {
      if (js == null || !js.has(...) || !js.has(...)) {
-         Log.w(TAG, "setContentByLocalJSON: nothing is avaiable");
+         Log.w(TAG, "setContentByLocalJSON: nothing is available");
+         return;  // ← 原来缺少 return，导致继续执行 js.getJSONObject() → NPE
      }
```

### 4. `TaskList.java` — 同上

```diff
  if (js == null || !js.has(...)) {
-     Log.w(TAG, "setContentByLocalJSON: nothing is avaiable");
+     Log.w(TAG, "setContentByLocalJSON: nothing is available");
+     return;
  }
```

### 5. `GTaskManager.java` — 增强日志

- `sync()` 增加步骤编号日志（1/4 login, 2/4 list, 3/4 syncing, 4/4 done）
- `syncContent()` 增加子步骤进度（文件夹、笔记、远程、清理）
- 异常消息包含具体类型和详细信息
- 新增 `this.asyncTask` 字段，使 `syncContent()` 可以发布详细的子步骤进度

**MetaData 数据流**：

```
MetaData 存储:
  setMeta(gid, metaInfo) → setNotes(metaInfo.toString())  // 直接存 JSON 字符串
                           setName("[META INFO] DON'T UPDATE AND DELETE")

MetaData 序列化 (用于 GitHub 同步):
  getNotes() → 返回之前存储的 JSON 字符串 → 直接作为文件内容写入 .json 文件

MetaData 反序列化 (从 GitHub 读取):
  setContentByRemoteJSON(js) → setNotes(js.getString("notes"))
    → new JSONObject(getNotes()) → 提取 mRelatedGid
```

**效果**：

- 同步过程中 MetaData 文件正确创建和更新，不再抛出 `IllegalAccessError`
- 任何 `Error` 类型（OOM、NoClassDefFound 等）都被捕获，不会导致闪退
- `Task` / `TaskList` 的 null-content 输入得到正确处理，不再 NPE
- 同步进度展示更详细，排查问题更快

---

## 修改 25：同步机制新增"通话/短信文件夹"可选同步开关

**日期**：2026-06-13

**问题描述**：

当前同步机制会同步所有文件夹，包括通话便签文件夹（ID -2）和短信便签文件夹（ID -4）。这些文件夹中的内容由系统自动生成（来电/去电/收短信/发短信），数据量大且频繁变化，用户通常不需要在多设备间同步这些自动记录。

**方案**：在设置中添加同步选项开关 `pref_key_sync_call_sms`（默认关闭），用户可选择是否将通话和短信文件夹纳入同步。关闭时，这两个文件夹及其中所有便签对同步机制完全不可见（不推送到远程、不从远程拉取、不参与差异比较）。

**修改文件**：

| 文件 | 路径 | 操作 |
|------|------|------|
| preferences.xml | `Notesmaster/app/src/main/res/xml/preferences.xml` | 修改 |
| strings.xml (3 种语言) | `Notesmaster/app/src/main/res/values*/strings.xml` | 修改 |
| GTaskManager.java | `Notesmaster/app/src/main/java/net/micode/notes/gtask/remote/GTaskManager.java` | 修改 |

**修改内容**：

### 1. `preferences.xml` — 新增同步选项分类 + CheckBoxPreference

新增"同步选项"(Sync Options)分类，内含：
- `pref_key_sync_call_sms`：`CheckBoxPreference`，`defaultValue="false"`（默认不同步通话/短信文件夹）

### 2. 字符串资源（3 种语言）

| Key | EN | zh-CN |
|-----|-----|-------|
| `preferences_sync_options_title` | Sync Options | 同步选项 |
| `preferences_sync_call_sms_title` | Sync call & SMS folders | 同步通话和短信文件夹 |
| `preferences_sync_call_sms_summary` | When enabled, call notes and SMS notes folders will also be synced to remote | 启用后，通话便签和短信便签文件夹也会同步到远程 |

### 3. `GTaskManager.java` — 核心同步逻辑改为条件排除

**新增导入**：`SharedPreferences`

**新增 `isSyncCallSmsEnabled()` 辅助方法**：
```java
private boolean isSyncCallSmsEnabled() {
    if (mContext == null) return false;
    SharedPreferences prefs = mContext.getSharedPreferences(
            NotesPreferenceActivity.PREFERENCE_NAME, Context.MODE_PRIVATE);
    return prefs.getBoolean("pref_key_sync_call_sms", false);
}
```

**所有同步查询改为条件化**：

| 方法 | 开关关闭时 |
|------|-----------|
| `syncFolder()` 通话文件夹块 | 整个 try 块跳过，不查询也不同步 |
| `syncFolder()` 本地文件夹查询 | WHERE 增加 `AND _id<>-2 AND _id<>-4`，通话/短信文件夹不出现在文件夹同步游标中 |
| `syncContent()` 笔记查询 | WHERE 增加 `AND parent_id<>-2 AND parent_id<>-4`，通话/短信文件夹内的笔记不出现在笔记同步游标中 |
| `addLocalNode()` | `FOLDER_CALL_NOTE` 匹配仅在开关开启时生效 |
| `refreshLocalSyncId()` | WHERE 增加排除通话/短信文件夹 ID 和 parent_id 的条件，防止"missing gid"错误 |

**开关开启时**：所有查询恢复为原始行为（不附加排除条件），与修改前完全一致。

**数据流对比**：

```
开关关闭（默认）：
  同步引擎启动
    → 列出远程文件
    → 查询本地文件夹（自动跳过通话/短信文件夹）
    → 查询本地笔记（自动跳过通话/短信文件夹中的笔记）
    → 差异对比、上传、下载、删除
    → 刷新 sync_id（跳过通话/短信相关条目）
  通话/短信文件夹及笔记 → 完全不可见 ✅

开关开启：
  同步引擎启动
    → 列出远程文件（包含 [MIUI_Notes]Call_Note 文件夹）
    → 查询本地所有文件夹和笔记（含通话/短信）
    → 通话/短信内容参与完整同步流程
  行为与修改前完全一致 ✅
```

**效果**：

- 默认不同步通话和短信便签，减少不必要的网络流量和远程仓库空间占用
- 用户可在设置中手动开启，需要同步时也可纳入
- 开关关闭时通话/短信文件夹对同步引擎完全透明，不参与任何差异计算
- 开关状态即时生效，下次同步时应用

---

## 修改 25 补充：清空 GitHub 同步仓库避免脏数据

**日期**：2026-06-13

**问题描述**：

远程同步仓库 `https://github.com/CheShuC/xiaomibianqian` 中残留大量历史同步数据（70 个文件），包括通话记录文件夹和已删除的测试文件夹。在"通话/短信文件夹不同步"修改后，这些远程脏数据可能导致首次同步时产生预期外的本地新增。

**操作**：

```bash
git clone https://github.com/CheShuC/xiaomibianqian.git
git rm -rf .                           # 删除所有 70 个文件
git commit -m "Clear repo: remove all synced data"
git push origin main --force
```

**删除的内容**：
- `[MIUI_Notes]Call_Note/` — 31 个通话便签 JSON 文件
- `[MIUI_Notes]Default/` — 7 个便签 JSON 文件
- `[MIUI_Notes]METADATA/` — 28 个元数据 JSON 文件
- `[MIUI_Notes]宝宝/` — 用户自定义文件夹（空）
- `[MIUI_Notes]测试/` — 用户自定义文件夹（空）

**效果**：

- 远程仓库完全清空，无任何文件残留
- 首次同步时 `GitHubSyncClient` 检测到空仓库，自动创建必要的文件夹结构（`.gitkeep`）
- 避免脏数据污染本地数据库

---

## 修改 25 补充 2：修复同步崩溃 — "cannot add remote task"

**日期**：2026-06-13

**问题描述**：

用户报告同步时报错 "cannot add remote task"，同步失败。错误来自 `GTaskManager.addRemoteNode()` 第 700 行。

**根因分析**：

`addRemoteNode()` 尝试将本地笔记推送到远程时，需要查找笔记父文件夹的远程 gid 映射：

```java
String parentGid = mNidToGid.get(sqlNote.getParentId());
if (parentGid == null) {
    throw new SyncException("cannot add remote task");
}
```

当笔记的 `parent_id = -1`（`ID_TEMPARAY_FOLDER`，临时/无文件夹笔记）时，临时文件夹从未参与同步（无 gid 映射），`mNidToGid.get(-1)` 返回 null → **崩溃**。

**对比查询**：

| 文件夹 | ID | 原笔记查询 | 是否有 gid 映射 |
|--------|----|-----------|----------------|
| 根文件夹 | 0 | ✅ 包含 | ✅ 有（`syncFolder` 处理） |
| 临时文件夹 | -1 | ❌ 未排除 | ❌ 无（从不参与同步） |
| 通话文件夹 | -2 | ✅ 已排除（修改 25） | — |
| 回收站 | -3 | ✅ 已排除 | — |
| 短信文件夹 | -4 | ✅ 已排除（修改 25） | — |

`ID_TEMPARAY_FOLDER (-1)` 是唯一的漏网之鱼——笔记查询未排除，但父文件夹无 gid 映射。这是**预存 bug**，在 Google Tasks 同步时代就已存在，只是 GitHub 同步改造后才被实际触发。

**修改文件**：

| 文件 | 路径 |
|------|------|
| GTaskManager.java | `Notesmaster/app/src/main/java/net/micode/notes/gtask/remote/GTaskManager.java` |

**修改内容**：

### 1. `syncContent()` 笔记查询 — 始终排除 `ID_TEMPARAY_FOLDER (-1)`

两路分支均添加 `parent_id <> -1` 条件：

```diff
- noteWhere = "(type=? AND parent_id<>?)"
+ noteWhere = "(type=? AND parent_id<>? AND parent_id<>?)"

  noteWhereArgs = new String[] {
      String.valueOf(Notes.TYPE_NOTE),
      String.valueOf(Notes.ID_TRASH_FOLER),
+     String.valueOf(Notes.ID_TEMPARAY_FOLDER)
  };
```

### 2. `refreshLocalSyncId()` — 同步 ID 刷新同样排除

```diff
- refreshWhere = "(type<>? AND parent_id<>?)"
+ refreshWhere = "(type<>? AND parent_id<>? AND _id<>? AND parent_id<>?)"

  refreshWhereArgs = new String[] {
      String.valueOf(Notes.TYPE_SYSTEM),
      String.valueOf(Notes.ID_TRASH_FOLER),
+     String.valueOf(Notes.ID_TEMPARAY_FOLDER),
+     String.valueOf(Notes.ID_TEMPARAY_FOLDER)
  };
```

排除临时文件夹本身（`_id<>-1`）及其子笔记（`parent_id<>-1`）。

### 3. `addRemoteNode()` — 增强错误日志

```diff
- Log.e(TAG, "cannot find task's parent tasklist");
- throw new SyncException(SyncException.DATA_ERROR, "cannot add remote task");
+ Log.e(TAG, "cannot find task's parent tasklist: noteId=" + sqlNote.getId()
+         + " parentId=" + sqlNote.getParentId());
+ throw new SyncException(SyncException.DATA_ERROR,
+         "cannot add remote task: note #" + sqlNote.getId()
+         + " has unsynced parent folder #" + sqlNote.getParentId());
```

下次出现类似问题时，日志和异常消息会包含具体的 noteId 和 parentId，便于快速定位。

**效果**：

- 临时文件夹中的笔记不再参与同步，避免 "cannot add remote task" 崩溃
- 任何未来新增的系统文件夹（如 -5）漏网时，增强的日志消息可快速定位
- 构建通过 (`BUILD SUCCESSFUL`)

---

## 修改 26：同步文件名改为笔记内容 + JSON 新增类别和时间戳

**日期**：2026-06-13

**问题描述**：

同步到 GitHub 的文件名使用随机格式 `note_1781335851418_5337.json`，无法从文件名识别笔记内容。例如笔记内容为 "aaa"，同步到 GitHub 后文件名却是 `note_时间戳_随机数.json`，不直观。同时 JSON 文件内容缺少类别和时间戳信息，在 GitHub 上浏览时不方便。

**方案**：
1. **文件名**：从笔记片段（snippet/内容首行）生成，如 `aaa.json`，保留中文字符
2. **JSON 文件**：新增 `sync_category`（笔记类别）和 `sync_timestamp`（创建/修改时间）字段

**修改文件**：

| 文件 | 路径 | 操作 |
|------|------|------|
| GitHubSyncClient.java | `Notesmaster/app/src/main/java/net/micode/notes/sync/github/GitHubSyncClient.java` | 修改 |
| Task.java | `Notesmaster/app/src/main/java/net/micode/notes/gtask/data/Task.java` | 修改 |

**修改内容**：

### 1. `GitHubSyncClient.java` — 文件名从笔记内容生成

**`createItem()` 重写文件名生成逻辑**：

```diff
- // Generate a unique filename
- String filename;
- if (!TextUtils.isEmpty(gid)) {
-     String baseName = gid.replaceAll("[^a-zA-Z0-9_\\-]", "_");
-     filename = baseName + ".json";
- } else {
-     filename = "note_" + System.currentTimeMillis() + "_" + (int)(Math.random() * 10000) + ".json";
- }
+ // Generate filename from note content (snippet), not random
+ if (!TextUtils.isEmpty(gid)) {
+     // Existing: extract filename from gid path
+     ...
+ } else {
+     // New note: use the note's name/snippet as filename
+     String noteName = item.getName();
+     if (!TextUtils.isEmpty(noteName)) {
+         String sanitized = sanitizeFilename(noteName.trim());
+         filename = sanitized + ".json";
+     } else {
+         filename = "note_" + System.currentTimeMillis() + ".json";
+     }
+ }
```

当笔记没有 gid（首次同步）时，从 `item.getName()`（笔记片段）生成文件名，而非随机时间戳。

**新增 `sanitizeFilename()` 工具方法**：

```java
private static String sanitizeFilename(String name) {
    String sanitized = name
            .replaceAll("[/\\\\:*?\"<>|#%&{}~^\\s]+", "_")
            .replaceAll("_+", "_")
            .replaceAll("^_|_$", "");
    if (sanitized.length() > 100) {
        sanitized = sanitized.substring(0, 100);
    }
    return sanitized;
}
```

- 保留中文、日文等 Unicode 字符（GitHub API 支持）
- 替换文件系统不允许的字符（`/ \ : * ? " < > |` 等）和空白字符为下划线
- 限制最大长度 100 字符，避免路径过长

### 2. `Task.java` — JSON 新增 `sync_category` 和 `sync_timestamp`

**`getLocalJSONFromContent()` 两路分支均新增字段**：

**新笔记（首次同步，`mMetaInfo == null`）**：
```json
{
  "meta_note": { "type": 0 },
  "meta_data": [{ "content": "aaa", "mime_type": "vnd.android.cursor.item/text_note" }],
  "sync_category": "text_note",
  "sync_timestamp": 1781335851418
}
```

**已同步笔记（`mMetaInfo != null`）**：
- `sync_category`：从 `meta_data[0].mime_type` 提取简短类别名（如 `text_note`、`call_note`、`sms_note`）
- `sync_timestamp`：从 `meta_note.created_date` 获取原始创建时间戳

**新增 `extractCategory()` 辅助方法**：
```java
private static String extractCategory(String mimeType) {
    // "vnd.android.cursor.item/text_note" → "text_note"
    int lastSlash = mimeType.lastIndexOf('/');
    return mimeType.substring(lastSlash + 1);
}
```

**JSON 文件示例（GitHub 上查看）**：

```json
{
  "meta_note": {
    "type": 0,
    "created_date": 1781335851418
  },
  "meta_data": [
    {
      "mime_type": "vnd.android.cursor.item/text_note",
      "content": "今天要买的东西",
      "data1": "0"
    }
  ],
  "sync_category": "text_note",
  "sync_timestamp": 1781335851418
}
```

**文件命名对比**：

| 笔记内容 | 修改前 | 修改后 |
|----------|--------|--------|
| aaa | `note_1781335851418_5337.json` | `aaa.json` |
| 今天要买的东西 | `note_1781335851419_1234.json` | `今天要买的东西.json` |
| 空笔记 | `note_1781335851420_5678.json` | `note_1781335851420.json` |

**效果**：

- GitHub 仓库中文件名直观反映笔记内容，浏览和搜索更方便
- JSON 文件包含 `sync_category`（笔记类型）和 `sync_timestamp`（时间戳），方便排序和分类
- 中文字符完整保留在文件名中
- 向后兼容：已有笔记更新时保留原路径，仅首次同步用新命名
- 构建通过 (`BUILD SUCCESSFUL`)

---

## 修改 27：同步机制分离为上传/下载模式 + 文件名取笔记首行

**日期**：2026-06-13

**问题描述**：

1. 双向同步机制导致不想要的便签每次同步都从远程写回本地，用户无法选择只上传或只下载
2. 文件名使用笔记全文（含换行），太长且无法识别。例如 `aaa\nbbb\nccc` → 文件名是 `aaa_bbb_ccc.json`，应该只取首行 `aaa.json`

**方案**：
1. **同步分离**：在 `doContentSync()` 加一层方向过滤，根据 syncMode 跳过不允许的方向
2. **文件名取首行**：`getName()` 返回完整笔记文本，修改为取 `\n` 前的内容

### 一、同步模式分离

**修改文件**（7 个）：

| 文件 | 路径 | 操作 |
|------|------|------|
| GTaskManager.java | `Notesmaster/app/src/main/java/net/micode/notes/gtask/remote/GTaskManager.java` | 修改 |
| GTaskASyncTask.java | `Notesmaster/app/src/main/java/net/micode/notes/gtask/remote/GTaskASyncTask.java` | 修改 |
| GTaskSyncService.java | `Notesmaster/app/src/main/java/net/micode/notes/gtask/remote/GTaskSyncService.java` | 修改 |
| NotesListActivity.java | `Notesmaster/app/src/main/java/net/micode/notes/ui/NotesListActivity.java` | 修改 |
| NotesPreferenceActivity.java | `Notesmaster/app/src/main/java/net/micode/notes/ui/NotesPreferenceActivity.java` | 修改 |
| preferences.xml | — | 不变（无新增 UI） |
| strings.xml (3 语言) | `Notesmaster/app/src/main/res/values*/strings.xml` | 修改 |

**三种同步模式**：

| 模式 | 常量 | 值 | 行为 |
|------|------|----|------|
| 双向同步 | `SYNC_MODE_BIDIRECTIONAL` | 0 | 原有行为不变 |
| 仅上传 | `SYNC_MODE_UPLOAD_ONLY` | 1 | 仅 local→remote，跳过 `ADD_LOCAL`/`UPDATE_LOCAL`/`DEL_LOCAL` |
| 仅下载 | `SYNC_MODE_DOWNLOAD_ONLY` | 2 | 仅 remote→local，跳过 `ADD_REMOTE`/`UPDATE_REMOTE`/`DEL_REMOTE`/`UPDATE_CONFLICT` |

**修改内容**：

#### 1. `GTaskManager.java` — 核心过滤

```java
// 新增常量
public static final int SYNC_MODE_BIDIRECTIONAL = 0;
public static final int SYNC_MODE_UPLOAD_ONLY = 1;
public static final int SYNC_MODE_DOWNLOAD_ONLY = 2;

// sync() 方法签名变更
public int sync(Context context, GTaskASyncTask asyncTask, int syncMode)

// doContentSync() 入口新增方向过滤
if (mSyncMode == SYNC_MODE_UPLOAD_ONLY && isPullAction(syncType)) return;
if (mSyncMode == SYNC_MODE_DOWNLOAD_ONLY && isPushAction(syncType)) return;
```

新增 `isPushAction()` / `isPullAction()` 工具方法。

关键设计：`doContentSync()` 是所有 push/pull 操作的唯一汇聚点（垃圾桶处理、文件夹同步、笔记同步、剩余远程项处理均经过此方法），在此加一层过滤即可覆盖所有调用路径。

#### 2. `GTaskASyncTask.java` — 传递模式

- 构造函数加 `int syncMode` 参数
- `doInBackground()` 传递给 `GTaskManager.sync(ctx, this, syncMode)`

#### 3. `GTaskSyncService.java` — Intent 传递

- 新增 `EXTRA_SYNC_MODE` 常量
- `startSync(Activity, int syncMode)` 方法加 mode 参数，写入 Intent extra
- `onStartCommand()` 提取 bundle 中的 mode 并传递

#### 4. `NotesListActivity.java` — 选择对话框

点击同步按钮时弹出 3 选项对话框（`AlertDialog.Builder.setItems()`），用户选择后调用 `GTaskSyncService.startSync(activity, mode)`。

#### 5. `NotesPreferenceActivity.java` — 设置页按钮兼容

设置页"立即同步"按钮使用默认 `SYNC_MODE_BIDIRECTIONAL`（不弹窗，保持便捷性）。

**用户交互流程**：

```
主列表 → 点击"同步"
  → 弹出对话框：
      • 双向同步
      • 仅上传（保存到远程）
      • 仅下载（从远程读取）
  → 用户选择 → 开始同步
```

#### 6. 字符串资源（3 种语言）

| Key | EN | zh-CN |
|-----|-----|-------|
| `sync_mode_bidirectional` | Bidirectional sync | 双向同步 |
| `sync_mode_upload` | Upload only (Save to remote) | 仅上传（保存到远程） |
| `sync_mode_download` | Download only (Read from remote) | 仅下载（从远程读取） |

### 二、文件名取首行

**修改文件**：`GitHubSyncClient.java`

**修改前**：
```java
String noteName = item.getName();  // 返回完整笔记文本
String sanitized = sanitizeFilename(noteName.trim());
```

多行笔记如 `"今天要买的东西\n- 牛奶\n- 面包"` → 文件名 `"今天要买的东西_-_牛奶_-_面包.json"`（所有换行被替换为下划线）。

**修改后**：
```java
String firstLine = noteName.trim();
int newlineIdx = firstLine.indexOf('\n');
if (newlineIdx > 0) {
    firstLine = firstLine.substring(0, newlineIdx).trim();
}
String sanitized = sanitizeFilename(firstLine);
```

`"今天要买的东西\n- 牛奶\n- 面包"` → 文件名 `"今天要买的东西.json"`。

**文件名对比**：

| 笔记内容 | 修改前 | 修改后 |
|----------|--------|--------|
| `aaa` | `note_timestamp_random.json` | `aaa.json` |
| `今天买\n牛奶\n面包` | `今天买_牛奶_面包.json` | `今天买.json` |

**数据流**：

```
用户点击同步 → 选择模式
  → GTaskSyncService.startSync(activity, mode)
    → Intent: ACTION_START_SYNC + EXTRA_SYNC_MODE
      → GTaskASyncTask(syncMode)
        → GTaskManager.sync(ctx, task, syncMode) → mSyncMode = syncMode
          → initGTaskList() → 加载远程状态
          → syncContent() → syncFolder() → doContentSync(动作, ...)
            → 方向过滤: 上传模式跳过 pull / 下载模式跳过 push
```

**效果**：

- 用户可选择只上传（备份）或只下载（恢复），避免不想要的远程笔记污染本地
- 主列表点击同步弹窗选择，设置页快捷按钮默认双向同步
- 多行笔记文件名清爽，仅为首行标题
- 构建通过 (`BUILD SUCCESSFUL`)

---

## 修改 27 补充：上传改为全量覆盖 + 下载修复增量模式

**日期**：2026-06-13

**问题描述**：

1. 上传模式仅上传本地差异（增量），用户期望的是覆盖远程仓库（清空后全量上传）
2. 下载模式崩溃——`refreshLocalSyncId()` 查询到无 gid 的本地笔记后无法在远程映射中找到匹配 → "something is missed" 异常
3. 下载模式会删除本地独有笔记（`DEL_LOCAL`），用户期望增量下载（不删本地）

**修改文件**：`GTaskManager.java`

**修改内容**：

### 1. 上传模式：覆盖远程仓库

在 `sync()` 方法中，上传模式先执行两个预处理步骤：

**`clearAllRemote(SyncClient)`** — 清空远程所有文件：
```java
// 遍历所有远程容器，删除每个容器内的文件和 .gitkeep
JSONArray containers = syncClient.listContainers();
for (container : containers) {
    JSONArray items = syncClient.listItems(containerId);
    for (item : items) syncClient.deleteItem(item.getId());
    syncClient.deleteItem(containerId + "/.gitkeep");
}
```

**`resetLocalSyncMetadata()`** — 清除本地所有便签的 gid/sync_id：
```java
ContentValues values = new ContentValues();
values.put(NoteColumns.GTASK_ID, "");
values.put(NoteColumns.SYNC_ID, 0);
mContentResolver.update(CONTENT_NOTE_URI, values, "type<>?", [TYPE_SYSTEM]);
```

清空后，所有本地便签在上传阶段表现为"新增"（无 gid），全部重新上传到远程仓库。

### 2. 下载模式：增量不删本地

**`doContentSync()` 新增过滤**：
```java
// 下载模式额外跳过 DEL_LOCAL（不删本地笔记）
if (mSyncMode == SYNC_MODE_DOWNLOAD_ONLY
        && syncType == Node.SYNC_ACTION_DEL_LOCAL) {
    return;
}
```

下载模式仅执行 `ADD_LOCAL`（新增）和 `UPDATE_LOCAL`（更新），不删除本地笔记。

### 3. 下载模式修复：`refreshLocalSyncId()` 跳过无 gid 笔记

**修改前**：查询所有非系统笔记，逐个在远程映射中查找 gid。本地独有笔记（从未上传，无 gid）→ `mGTaskHashMap.get("")` 返回 null → 抛出 "something is missed" 异常。

**修改后**：WHERE 子句添加 `gtask_id!='' AND gtask_id IS NOT NULL`，仅刷新已有远程映射的笔记的 sync_id。

**三种模式行为总结**：

| 操作 | 双向同步 | 仅上传 | 仅下载 |
|------|---------|--------|--------|
| 上传新增笔记 | ✅ | ✅ (全量覆盖) | ❌ |
| 上传修改笔记 | ✅ | ✅ | ❌ |
| 上传删除笔记 | ✅ | ❌ (不删远程) | ❌ |
| 下载新增笔记 | ✅ | ❌ | ✅ |
| 下载更新笔记 | ✅ | ❌ | ✅ |
| 下载删除笔记 | ✅ | ❌ | ❌ (增量不删) |
| 清空远程仓库 | ❌ | ✅ (预处理) | ❌ |

**效果**：

- 上传模式：先清空远程仓库 → 清除本地 gid → 全量上传覆盖
- 下载模式：增量下载（仅新增+更新），不删除本地笔记
- 下载模式不再崩溃

---

## 修改 28：同步机制精简 — 仅保留上传/下载 + 通话/短信永久排除 + 仓库清理

**日期**：2026-06-13

**问题描述**：

原有同步机制存在三个问题：
1. 双向同步模式在实际使用中容易导致不想要的远程笔记污染本地数据，用户实际只需要"备份到远程"和"从远程恢复"两种场景
2. 通话/短信文件夹的同步开关（修改 25）增加了一层不必要的配置复杂度，这些自动生成的便签永远不需要跨设备同步
3. 远程仓库 `xiaomibianqian` 中残留历史同步数据

**方案**：
1. **移除双向同步**：对话框仅保留"上传"和"下载"两个选项。设置页"立即同步"按钮默认执行上传（备份场景）
2. **通话/短信永久排除**：移除设置中的复选框，将排除逻辑硬编码到 `GTaskManager` 所有同步查询中。同时 `GitHubSyncClient` 不再创建 `Call_Note` 容器
3. **清空远程仓库**：删除所有历史文件并强制推送，确保下次同步从干净状态开始

**修改文件**：

| 文件 | 路径 | 操作 |
|------|------|------|
| GTaskManager.java | `Notesmaster/app/src/main/java/net/micode/notes/gtask/remote/GTaskManager.java` | 修改 |
| NotesListActivity.java | `Notesmaster/app/src/main/java/net/micode/notes/ui/NotesListActivity.java` | 修改 |
| NotesPreferenceActivity.java | `Notesmaster/app/src/main/java/net/micode/notes/ui/NotesPreferenceActivity.java` | 修改 |
| preferences.xml | `Notesmaster/app/src/main/res/xml/preferences.xml` | 修改 |
| GitHubSyncClient.java | `Notesmaster/app/src/main/java/net/micode/notes/sync/github/GitHubSyncClient.java` | 修改 |
| strings.xml (3 种语言) | `Notesmaster/app/src/main/res/values*/strings.xml` | 修改 |

**修改内容**：

### 1. `GTaskManager.java` — 核心逻辑硬编码排除

**移除内容**：
- `isSyncCallSmsEnabled()` 方法及其 `SharedPreferences` import
- 所有 `if (isSyncCallSmsEnabled())` 条件分支
- `addLocalNode()` 中 `FOLDER_CALL_NOTE` 匹配分支
- `syncFolder()` 中整个通话文件夹同步代码块

**硬编码排除**（始终生效）：
- `syncContent()` 笔记查询：`parent_id <> -2 AND parent_id <> -4`
- `syncFolder()` 文件夹查询：`_id <> -2 AND _id <> -4`
- `refreshLocalSyncId()` 刷新查询：排除通话/短信文件夹 ID 和 parent_id

### 2. `NotesListActivity.java` — 对话框仅 2 选项

```java
// 修改前：3 个选项（双向同步 / 仅上传 / 仅下载）
// 修改后：2 个选项（上传 / 下载）
final CharSequence[] items = new CharSequence[] {
    getString(R.string.sync_mode_upload),
    getString(R.string.sync_mode_download)
};
```

### 3. `NotesPreferenceActivity.java` — 默认上传模式

设置页"立即同步"按钮从 `SYNC_MODE_BIDIRECTIONAL` 改为 `SYNC_MODE_UPLOAD_ONLY`。

### 4. `preferences.xml` — 移除同步选项分类

- 删除 `pref_key_sync_call_sms` CheckBoxPreference
- "Sync Options" → "Appearance"（外观），仅保留背景色随机开关

### 5. `GitHubSyncClient.java` — 不创建 Call_Note 容器

`ensureDefaultContainers()` 从创建 3 个默认容器缩减为 2 个：
- ✅ `[MIUI_Notes]Default`
- ✅ `[MIUI_Notes]METADATA`
- ❌ `[MIUI_Notes]Call_Note`（已移除）

### 6. 字符串资源（3 种语言）

**移除的字符串**：

| Key | 原值 (EN) |
|-----|----------|
| `sync_mode_bidirectional` | Bidirectional sync |
| `preferences_sync_options_title` | Sync Options |
| `preferences_sync_call_sms_title` | Sync call & SMS folders |
| `preferences_sync_call_sms_summary` | When enabled... |

**新增的字符串**：

| Key | EN | zh-CN | zh-TW |
|-----|-----|-------|-------|
| `preference_category_appearance` | Appearance | 外观 | 外觀 |

**修改的字符串**（更简洁）：

| Key | 修改前 | 修改后 |
|-----|--------|--------|
| `sync_mode_upload` | Upload only (Save to remote) | Upload (Save to remote) |
| `sync_mode_download` | Download only (Read from remote) | Download (Read from remote) |

### 7. 远程仓库清理

```bash
git clone https://github.com/CheShuC/xiaomibianqian.git
git rm -rf .
git commit -m "Clear all synced data - refactored sync to upload/download only, excl. call/SMS"
git push origin main --force
```

删除的文件（7 个）：
- `[MIUI_Notes]Call_Note/.gitkeep`
- `[MIUI_Notes]Default/.gitkeep`
- `[MIUI_Notes]English/.gitkeep`
- `[MIUI_Notes]METADATA/.gitkeep`
- `[MIUI_Notes]METADATA/[META_INFO]_DON'T_UPDATE_AND_DELETE.json`
- `[MIUI_Notes]中午/.gitkeep`
- `[MIUI_Notes]中午/9999999.json`

**同步行为总结**：

| 操作 | 上传模式 | 下载模式 |
|------|---------|--------|
| 上传新增笔记 | ✅ 全量覆盖 | ❌ |
| 上传修改笔记 | ✅ | ❌ |
| 上传删除笔记 | ❌ (不删远程) | ❌ |
| 下载新增笔记 | ❌ | ✅ 增量 |
| 下载更新笔记 | ❌ | ✅ 增量 |
| 下载删除笔记 | ❌ | ❌ (不删本地) |
| 清空远程仓库 | ✅ (预处理) | ❌ |
| 通话/短信文件夹 | ❌ 永久排除 | ❌ 永久排除 |

**用户交互流程**：

```
主列表 → 点击"同步"
  → 弹出对话框：
      • 上传（保存到远程）
      • 下载（从远程读取）
  → 用户选择 → 开始同步

设置页 → 点击"立即同步"
  → 直接执行上传（备份到远程仓库）
```

**效果**：

- 同步对话框精简为 2 选项，更符合"备份/恢复"的使用场景
- 通话和短信便签永久排除在同步之外，无需用户手动配置
- 设置页更简洁，"外观"分类取代"同步选项"分类
- 远程仓库完全清空，下次同步从干净状态开始
- 构建通过 (`BUILD SUCCESSFUL`)

---

## 修改 28 补充：修复下载模式"同步成功但无文件"（2 个 Bug）

**日期**：2026-06-13

**问题描述**：

用户报告下载模式有概率显示"同步成功"但本地没有产生任何文件。

**根因分析**：

经代码追踪发现两个独立 Bug：

### Bug 1：`Task.getLocalJSONFromContent()` 在无 MetaData 时丢弃完整内容

**调用链**：
```
initGTaskList() → listItems() → 每个文件内容存入 getNotes()
  → setMetaInfo() → 如果 MetaData 不存在，mMetaInfo 保持 null
    → addLocalNode() → getLocalJSONFromContent()
      → mMetaInfo == null 分支：仅用 getName()（文件名首行）创建最简 JSON
      → getNotes() 中的完整 JSON（含正文、颜色、提醒等元数据）被完全忽略
```

`getName()` 仅返回文件名首行文字（如 "今天买"），而 `getNotes()` 包含从 GitHub 下载的完整文件内容。当 `mMetaInfo` 为 null 时，原代码走 `mMetaInfo == null` 分支，仅用 name 构建一个最简 JSON，丢弃了所有内容。

虽然 `isWorthSaving()` 仍返回 true（因为 name 非空），生成的便签只有标题没有正文，用户体验为"同步成功但无内容"。

### Bug 2：`initGTaskList()` 内层循环覆写容器 gid

```java
// 外层循环：gid = 容器路径（如 "[MIUI_Notes]Default"）
for (int i = 0; i < jsTaskLists.length(); i++) {
    String gid = object.getString(GTaskStringUtils.GTASK_JSON_ID);  // 容器 gid
    JSONArray jsTasks = syncClient.listItems(gid);  // 用容器 gid 列出文件
    for (int j = 0; j < jsTasks.length(); j++) {
        gid = object.getString(GTaskStringUtils.GTASK_JSON_ID);  // ❌ 覆写为文件路径
        mGTaskHashMap.put(gid, task);
    }
    // gid 现在是最后一个文件的路径，不再是容器路径
}
```

当前代码结构下此覆写未触发实际错误（外层循环后续不再使用 gid），但属于潜在风险。

**修改文件**：

| 文件 | 路径 | 操作 |
|------|------|------|
| Task.java | `Notesmaster/app/src/main/java/net/micode/notes/gtask/data/Task.java` | 修改 |
| GTaskManager.java | `Notesmaster/app/src/main/java/net/micode/notes/gtask/remote/GTaskManager.java` | 修改 |

**修改内容**：

### 1. `Task.java` — `getLocalJSONFromContent()` 重构

**核心修复**：在 `mMetaInfo == null` 时，先尝试将 `getNotes()` 解析为 JSON 作为 `sourceJson`：

```java
// 当 mMetaInfo 为 null 时，尝试从 getNotes() 重建
JSONObject sourceJson = mMetaInfo;
if (sourceJson == null && getNotes() != null && getNotes().trim().length() > 0) {
    try {
        sourceJson = new JSONObject(getNotes());
    } catch (JSONException e) {
        Log.w(TAG, "getNotes is not valid JSON, falling back to minimal note");
    }
}
```

如果 `getNotes()` 包含有效 JSON（下载的文件内容），则作为 `sourceJson` 使用与 `mMetaInfo != null` 完全相同的处理路径，正确提取 `meta_note`、`meta_data` 等结构。同时缓存重建的 JSON 到 `mMetaInfo` 以供后续使用。

如果解析失败，才回退到原来的最简路径（仅用 name 构建）。

### 2. `GTaskManager.java` — gid 变量隔离 + 同步计数

**gid 修复**：内层循环改用独立变量 `itemGid`，不再覆写外层容器 gid：

```diff
- gid = object.getString(GTaskStringUtils.GTASK_JSON_ID);
+ String itemGid = object.getString(GTaskStringUtils.GTASK_JSON_ID);
```

**同步计数**：新增 `mSyncCountAdded` / `mSyncCountUpdated` / `mSyncCountDeleted` 三个计数器，在 `addLocalNode()` 和 `addRemoteNode()` 中递增。同步完成后根据计数生成摘要消息：

| 情况 | 显示消息 |
|------|---------|
| 下载有新增 | "新增 N 条" |
| 上传有新增 | "新增 N 条" |
| 无任何变更（下载） | "远程仓库无新文件可下载" |
| 无任何变更（上传） | "无新笔记需上传" |

**数据流修复对比**：

```
修复前（Bug 1）：
  GitHub 文件内容 (完整 JSON)
    → setContentByRemoteJSON() → setNotes(fullJson)
      → mMetaInfo == null（无 MetaData）
        → getLocalJSONFromContent()
          → ❌ 忽略 getNotes()，仅用 getName()（文件名）
            → 生成 {meta_note:{}, meta_data:[{content: "文件名"}]}
              → 便签内容丢失，只有标题

修复后：
  GitHub 文件内容 (完整 JSON)
    → setContentByRemoteJSON() → setNotes(fullJson)
      → mMetaInfo == null（无 MetaData）
        → getLocalJSONFromContent()
          → ✅ 解析 getNotes() → sourceJson = 完整 JSON
            → 提取 meta_note, meta_data, sync_category, sync_timestamp
              → 便签完整还原 ✅
```

**效果**：

- 下载模式从 GitHub 仓库下载的笔记完整保留正文、颜色、提醒等元数据
- "同步成功但无文件"问题修复（两种情况：Bug 1 修复后内容正确还原；空仓库明确提示"远程仓库无新文件可下载"）
- `initGTaskList()` 变量安全隔离，消除潜在数据错乱风险
- 同步完成后显示具体的新增/更新/删除条数
- 构建通过 (`BUILD SUCCESSFUL`)

---

## 修改 28 补充 2：修复 `ensureDefaultContainers` 吞异常 + 增强诊断日志

**日期**：2026-06-13

**问题描述**：

修改 28 补充修复了 Task 反序列化问题后，用户仍然无法下载。日志显示网络层 `E2BIG` 错误但同步流程未报告任何异常。

**根因分析**：

### 致命缺陷：`ensureDefaultContainers()` 静默吞下所有异常

```java
// 修改前
private void ensureDefaultContainers() throws SyncException {
    for (String folderName : defaults) {
        try {
            putFile(path, "", "Sync: initialize " + folderName, null);
        } catch (Exception e) {
            Log.w(TAG, "Could not create " + folderName + ": " + e.getMessage());
            // ← 异常被吞！不抛给调用者
        }
    }
}
```

`putFile()` 可能因为网络错误、认证失败、仓库不存在等原因失败。原代码捕获所有异常后仅打一行 Log.w，不重新抛出。导致：

```
listContainers() → 发现空仓库
  → ensureDefaultContainers()
    → putFile() 失败（网络/auth/...）→ 异常被吞
  → 重新 list → 仍然为空（容器未创建）
  → 未检查第二次 list 结果 → 返回 0 个容器
    → initGTaskList() → 0 个 task list → 无任何节点
      → syncContent() → 无任何内容可同步
        → "同步成功" 但实际什么都没做 ❌
```

### 次因：`listContainers()` 不验证初始化结果

第二次 list 后未检查 `containers.length()`，即使仍为 0 也继续执行。

**修改文件**：

| 文件 | 路径 | 操作 |
|------|------|------|
| GitHubSyncClient.java | `Notesmaster/app/src/main/java/net/micode/notes/sync/github/GitHubSyncClient.java` | 修改 |
| GTaskManager.java | `Notesmaster/app/src/main/java/net/micode/notes/gtask/remote/GTaskManager.java` | 修改 |

**修改内容**：

### 1. `GitHubSyncClient.ensureDefaultContainers()` — 异常不再吞没

```java
// 修改后：收集所有错误，任一失败则抛出 SyncException
int created = 0;
StringBuilder errors = new StringBuilder();
for (String folderName : defaults) {
    try {
        putFile(path, "", ...);
        created++;
    } catch (Exception e) {
        errors.append(folderName).append(": ").append(e.getMessage()).append("; ");
    }
}
if (errors.length() > 0) {
    throw new SyncException(SyncException.NETWORK_ERROR,
            "Failed to create default containers: " + errors.toString());
}
```

现在如果 GitHub API 不可达、Token 权限不足、仓库不存在等情况，同步会**明确失败并显示具体错误**，而非静默"成功"。

### 2. `GitHubSyncClient.listContainers()` — 验证初始化结果

`ensureDefaultContainers()` 后重新 list，如果容器数仍为 0，抛出明确的诊断异常：

```java
if (containers.length() == 0) {
    throw new SyncException(SyncException.DATA_ERROR,
            "Repository has no containers after initialization. "
            + "Check that the GitHub token has repo scope and the "
            + "repository " + mOwner + "/" + mRepo + " exists.");
}
```

### 3. 增强全链路诊断日志

| 方法 | 新增日志 |
|------|---------|
| `listContainers()` | `"listContainers: found N containers"` |
| `listItems()` | `"listItems: found N items in {containerId}"` |
| `initGTaskList()` | 入口 `"got N containers from remote"`，出口 `"loaded N task lists, N total nodes, N meta entries"` |
| `createItem()` | 跳过的 item 带上 name/gid 信息 |

**效果**：

- 容器创建失败时同步会明确报错（而非静默"成功"）
- 所有同步操作的节点数可见于 Logcat，便于排查
- 空仓库初始化失败能被准确诊断
- 构建通过 (`BUILD SUCCESSFUL`)

---

## 修改 28 补充 3：移除时间戳逻辑 — 上传/下载不再比较时间

**日期**：2026-06-13

**问题描述**：

同步流程中存在多处时间戳比较逻辑：
1. `Node.getSyncAction()` 比较本地 `sync_id` 与远程 `last_modified`，可能返回 `NONE`（跳过）或错误的同步方向
2. `listItems()` 将 `last_modified` 设为 `System.currentTimeMillis()`（假值），掩盖了真实问题
3. `getLocalJSONFromContent()` 在 JSON 中嵌入 `sync_timestamp`
4. `refreshLocalSyncId()` 在每个同步周期更新本地 `sync_id`

在仅上传/仅下载模式中，时间戳比较完全不需要：
- 上传模式：清除远程 → 全量上传 → 无需比较
- 下载模式：远程文件覆盖本地 → 无需比较

**修改文件**：

| 文件 | 路径 | 操作 |
|------|------|------|
| Task.java | `Notesmaster/app/src/main/java/net/micode/notes/gtask/data/Task.java` | 修改 |
| GitHubSyncClient.java | `Notesmaster/app/src/main/java/net/micode/notes/sync/github/GitHubSyncClient.java` | 修改 |
| GTaskManager.java | `Notesmaster/app/src/main/java/net/micode/notes/gtask/remote/GTaskManager.java` | 修改 |

**修改内容**：

### 1. `Task.getLocalJSONFromContent()` — 不再写入 `sync_timestamp`

移除两处 `sync_timestamp` 字段的写入：
- `mMetaInfo == null` 分支：删除 `js.put("sync_timestamp", System.currentTimeMillis())`
- `mMetaInfo != null` 分支：删除整个 `sync_timestamp` 赋值块

上传到 GitHub 的 JSON 文件不再包含冗余时间戳。

### 2. `GitHubSyncClient.listItems()` — `last_modified` 固定为 0

```diff
- item.put(GTaskStringUtils.GTASK_JSON_LAST_MODIFIED, System.currentTimeMillis());
+ item.put(GTaskStringUtils.GTASK_JSON_LAST_MODIFIED, 0L);
```

不再伪造当前时间为"最后修改时间"。

### 3. `GTaskManager` — 匹配项跳过 `getSyncAction()` 比较

`syncContent()` 和 `syncFolder()` 两处，对已匹配（本地与远程 gid 相同）的节点：

```java
// 修改前：调用 getSyncAction() 比较时间戳
syncType = node.getSyncAction(c);

// 修改后：直接按同步模式决定方向
if (mSyncMode == SYNC_MODE_UPLOAD_ONLY) {
    syncType = Node.SYNC_ACTION_UPDATE_REMOTE;   // 始终推送
} else if (mSyncMode == SYNC_MODE_DOWNLOAD_ONLY) {
    syncType = Node.SYNC_ACTION_UPDATE_LOCAL;    // 始终拉取
} else {
    syncType = node.getSyncAction(c);            // 双向模式保留原逻辑
}
```

### 4. `GTaskManager.syncContent()` — 跳过 `refreshLocalSyncId()`

```java
// 仅双向同步模式需要刷新 sync_id
if (!mCancelled && mSyncMode == SYNC_MODE_BIDIRECTIONAL) {
    refreshLocalSyncId();
}
```

上传/下载模式不再维护 `sync_id` 字段。

**问题修复原理**：

修改前的时间戳比较导致的隐蔽问题：

| 本地状态 | 远程状态 | `getSyncAction` 返回值 | 上传模式 | 下载模式 |
|---------|---------|----------------------|---------|---------|
| `LOCAL_MODIFIED=1`, `sync_id == last_modified` | — | `UPDATE_REMOTE` | ✅ 推送 | ❌ 被过滤(推送动作) |
| `LOCAL_MODIFIED=0`, `sync_id == last_modified` | — | `NONE` | ❌ 跳过 | ❌ 跳过 |
| `LOCAL_MODIFIED=0`, `sync_id != last_modified` | — | `UPDATE_LOCAL` | ❌ 被过滤(拉取动作) | ✅ 拉取 |

上传模式中 `NONE` 导致笔记被跳过未上传。下载模式中 `UPDATE_REMOTE` 被方向过滤跳过，`NONE` 也跳过，导致已修改或被篡改 `sync_id` 的本地笔记无法更新。

修改后：所有匹配的笔记在对应方向 100% 处理，不依赖时间戳。

**效果**：

- 上传模式：100% 覆盖远程（不再有笔记因时间戳被跳过）
- 下载模式：100% 覆盖本地匹配笔记（即使本地已修改也会被远程覆盖）
- JSON 文件更简洁，不含冗余时间戳
- `refreshLocalSyncId()` 在网络差时不再阻塞同步完成
- 构建通过 (`BUILD SUCCESSFUL`)

---

## 修改 29：同步引擎完全重写 — 从双向差量引擎到直接上传/下载

**日期**：2026-06-13

**问题描述**：

经过 4 轮 bug 修复（Task 反序列化、ensureDefaultContainers 吞异常、gid 覆写、时间戳比较），每次修复后暴露出新的边缘情况。根本原因：当前同步引擎是 Google Tasks 双向差量引擎（8 个哈希映射、8 种同步动作、多阶段对账循环），被强行适配到 GitHub 上传/下载系统。架构不匹配导致脆弱难以维护。

**方案**：完全重写 `GTaskManager.java`，用两个简洁的直线方法 `uploadAll()` 和 `downloadAll()` 替代所有复杂逻辑。同时删除无用的 `MetaData.java` 和 `Node.SYNC_ACTION_*` 常量。

**删除的代码**（~836 行）：

| 组件 | 行数 | 原因 |
|------|------|------|
| `MetaData.java` | 82 | 笔记 JSON 已自包含，无需侧车文件 |
| `Node.SYNC_ACTION_*` (8 常量) | 20 | 无动作分发 — 直接创建/更新 |
| `Node.getSyncAction()` 抽象方法 | 1 | 无时间戳比较 |
| `Task.getSyncAction()` | 50 | 无冲突检测 |
| `TaskList.getSyncAction()` | 30 | 无文件夹时间戳比较 |
| `Task.mMetaInfo` + `setMetaInfo()` | 15 | MetaData 已删除 |
| `GTaskManager` 6 个 HashMap + 多阶段方法 | ~600 | 由直线循环替代 |
| `GitHubSyncClient` MetaData instanceof 分支 | ~40 | MetaData 已删除 |
| **合计** | **~836** | — |

**修改文件**：

| 文件 | 操作 |
|------|------|
| `GTaskManager.java` | **重写**（1010→385 行） |
| `Task.java` | 编辑（删除 mMetaInfo/setMetaInfo/getSyncAction；简化 getLocalJSONFromContent） |
| `Node.java` | 编辑（删除 SYNC_ACTION_* 常量和 getSyncAction） |
| `TaskList.java` | 编辑（删除 getSyncAction） |
| `MetaData.java` | **删除** |
| `GitHubSyncClient.java` | 编辑（删除 MetaData 引用、METADATA 容器） |

**新架构**：

```
downloadAll()                           uploadAll()
    │                                       │
    ├─ listContainers()                     ├─ clearAllRemote()
    ├─ for each container:                  ├─ resetLocalSyncMetadata()
    │   ├─ ensureLocalFolder()              ├─ query local folders
    │   └─ listItems()                      ├─ for each: createContainer()
    │       └─ for each item:               ├─ query local notes
    │           ├─ parse JSON (Task)        ├─ for each:
    │           ├─ find/create SqlNote       │   ├─ serialize (Task)
    │           └─ commit to DB             │   ├─ createItem()
    │                                       │   └─ update local gid
    └─ Report count                         └─ Report count
```

**无哈希映射。无同步动作。无时间戳比较。无 MetaData。无对账阶段。**

**关键简化**：

1. **`Task.getLocalJSONFromContent()`** 不再有 `getName()` 语义区分 — 始终保留存储的 JSON 内容，永不覆盖
2. **上传即覆盖** — `clearAllRemote()` → `resetLocalSyncMetadata()` → 全量上传
3. **下载即增量** — 根据 gid 匹配查找已有笔记，有则更新，无则新建，不删除本地独有笔记
4. **文件夹同步** — 自定义文件夹自动创建为远程容器，下载时反向重建

**GitHub 仓库结构变更**：

修改前：
```
[MIUI_Notes]Default/    ← 笔记文件
[MIUI_Notes]METADATA/   ← 每个笔记的元数据侧车文件
```

修改后：
```
[MIUI_Notes]Default/    ← 笔记文件（自包含，含全部信息）
```

**效果**：

- 代码量减少 ~836 行（-30%）
- 同步引擎从 9 个方法简化为 2 个核心方法 + 4 个辅助方法
- 所有旧代码引用（SYNC_ACTION、MetaData、mMetaInfo 等）已完全清除
- 下载不再依赖 MetaData 和时间戳 — 每个 JSON 文件完全自包含
- 构建通过 (`BUILD SUCCESSFUL`)
