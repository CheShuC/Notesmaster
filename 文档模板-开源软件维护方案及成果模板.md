# **开源软件的维护方案及成果**

撰写人：车abcd

## 开源软件简介

NotesMaster（原名 MiCode Notes）是一款源自小米 MiCode 开源社区（www.micode.net）的 Android 便签应用，采用 Apache License 2.0 开源协议发布。该应用提供文本笔记编辑、通话记录便签、短信记录便签、文件夹分类管理、桌面小组件（Widget）、闹钟提醒、全文搜索、SD 卡备份导出以及通过 Google Tasks API 进行云端同步等功能。

| 属性 | 值 |
|---|---|
| **项目名称** | Notes-master (NotesMaster) |
| **开发者** | MiCode Open Source Community |
| **出处** | https://github.com/MiCode/Notes |
| **编程语言** | Java 11 |
| **构建系统** | Gradle 9.0.0 (Kotlin DSL) |
| **最低 SDK** | 24 (Android 7.0) |
| **目标 SDK** | 36 (Android 15) |
| **代码规模** | 约 10,001 行 Java 代码，39 个源文件，104 个资源文件 |
| **包结构** | 8 个主要包：data、model、tool、ui、widget、gtask.data、gtask.exception、gtask.remote |

## 软件维护需求描述

### 2.1 开源软件分析

通过对原始 MiCode Notes 开源代码的全面分析（含代码审查报告），发现该软件存在以下不足和待完善内容：

**（1）界面菜单显示缺陷**

原始代码中多处菜单配置文件缺少 `android:showAsAction` 属性。在 Android 3.0+（API 11+）的 ActionBar 机制中，缺少此属性的菜单项默认进入溢出菜单（overflow menu），用户需点击"更多"按钮才能看到操作选项。这导致：

- 主列表界面（`note_list.xml`）的 5 个菜单项全部隐藏：新建文件夹、导出文本、同步、设置、搜索
- 笔记编辑界面（`note_edit.xml`）的 8 个按钮全部隐藏：新建、删除、字体大小、清单模式、分享、发送到桌面、提醒、删除提醒
- 子文件夹界面（`sub_folder.xml`）缺少返回按钮（`menu_back`）和搜索按钮（`menu_search`），且唯一的"新建笔记"项也因缺少 `showAsAction` 而被隐藏

**（2）短信便签功能缺失**

原始代码中已预留 `ID_SMS_RECORD_FOLDER = -4` 常量，但未实现任何短信便签功能：没有 SmsReceiver 广播接收器、没有短信数据模型（SmsNote）、没有短信便签的 UI 展示和编辑功能、在 `ListEditState` 枚举和 `ROOT_FOLDER_SELECTION` 中均未包含短信文件夹。通话记录便签虽已实现但因依赖 MIUI 系统电话应用发送的 `ACTION_INSERT_OR_EDIT` Intent，在标准 Android 设备上无法工作。

**（3）闹钟提醒功能不可用（5 个崩溃点）**

- `AlarmAlertActivity` 使用了 Android API 17 已废弃的 `KeyguardLock` 和新版权限模型下失效的 `WakeLock` 模式，无法在锁屏状态下正常显示
- `AlarmReceiver` 未创建 `NotificationChannel`（API 26+ 必需），导致通知无法发出
- `DateTimePickerDialog` 仅支持时间选择，缺少日期选择功能
- `AlarmInitReceiver` 位置错误（放在 widget 包中）且未正确注册开机广播
- 三个关键权限未在代码中运行时请求：`POST_NOTIFICATIONS`（API 33+）、`SCHEDULE_EXACT_ALARM`（API 31+）、`USE_FULL_SCREEN_INTENT`（API 34+），导致闹钟界面无法弹出

**（4）搜索功能不可用（6 个问题）**

- `searchable.xml` 中 `searchSuggestAuthority="notes"` 与实际 ContentProvider 授权名 `"micode_notes"` 不匹配，搜索建议功能完全失效
- `ACTION_SEARCH` Intent 未在 `NoteEditActivity` 中被处理，用户回车提交搜索后 Activity 直接关闭
- `sub_folder.xml` 缺少搜索菜单项，进入子文件夹后无法触发搜索
- `AndroidManifest.xml` 存在 XML 格式错误（`</intent-filter >>` 多余字符）
- 缺少"无搜索结果"的用户提示字符串资源
- `search_result.png` 图标仅存在于 `drawable-hdpi` 一个密度目录

**（5）代码质量问题**

代码审查发现的 10 项主要质量问题：
- `NotesProvider.getType()` 方法未实现，返回 null
- `increaseNoteVersion` 方法存在 SQL 参数非参数化拼接风险
- `Contact.getContact()` 中 Cursor 泄露风险
- 多处常量拼写错误（如 `ID_TEMPARAY_FOLDER`、`ID_TRASH_FOLER`）
- 线程安全隐患（HashMap 并发访问）
- 广泛捕获 Exception 吞噬异常信息
- 缺少输入参数 null 校验
- JSON 类型不匹配时缺乏容错

### 2.2 开源软件维护需求

根据上述分析，确定以下 6 项维护需求：

**1. 修复隐藏的菜单栏**

在 `sub_folder.xml` 中补充返回按钮和搜索按钮，为所有菜单项添加 `android:showAsAction="ifRoom"` 属性，使工具栏按钮可见。

**2. 修复隐藏的便签操作按钮**

在 `note_edit.xml`、`call_note_edit.xml`、`note_list.xml`、`call_record_folder.xml` 等菜单文件中为所有菜单项添加 `showAsAction="ifRoom"` 属性，将隐藏的编辑操作按钮（删除、分享、字体、提醒等）显示在工具栏。

**3. 修复被隐藏的文件夹显示内容**

在 `NotesListActivity` 中新增 `SMS_RECORD_FOLDER` 状态支持，更新 `ROOT_FOLDER_SELECTION` 包含短信文件夹，使用户在列表中有短信便签时能看见并进入"短信便签"文件夹。

**4. 修复无法使用的闹钟提醒功能**

全面重写闹钟系统：将 `AlarmReceiver` 从直接启动 Activity 改为高优先级 Notification + FullScreenIntent 模式；为 `AlarmAlertActivity` 添加锁屏唤醒和全屏通知能力；在 `NoteEditActivity` 中实现三层权限检查流程；修复 `AlarmInitReceiver` 的开机重注册逻辑。

**5. 修复电话记录便签和短信记录便签**

新增 `CallReceiver`（BroadcastReceiver）+ `CallLogObserver`（ContentObserver）双重保障机制，使通话便签在标准 Android 设备上也能自动创建。仿照通话记录模型，完整实现短信便签的数据模型（`SmsNote`）、广播接收器（`SmsReceiver`）、ContentObserver（`SmsSentObserver`）、UI 展示和编辑功能。

**6. 修复无法使用的搜索栏**

将 `searchSuggestAuthority` 修正为 `micode_notes`；在 `NoteEditActivity` 中实现 `handleSearchIntent()` 方法处理 `ACTION_SEARCH`，查询数据库返回结果列表；为 `sub_folder.xml` 添加搜索菜单项；修复 `AndroidManifest.xml` XML 格式错误；添加"无搜索结果"提示；复制搜索图标到全部密度目录。

#### 用例图描述

下图展示了维护后新增的功能需求（以 UML 用例图描述）：

```
┌──────────────────────────────────────────────────────────────────────┐
│                        NotesMaster 系统                              │
│                                                                      │
│  ┌──────────────────────┐                                            │
│  │      普通用户         │                                            │
│  └──────────┬───────────┘                                            │
│             │                                                        │
│    ┌────────┴──────────────────────────────────────┐                │
│    │                                                │                │
│    ▼                                                ▼                │
│  ┌──────────────┐                          ┌──────────────┐         │
│  │  查看菜单栏   │                          │  编辑便签     │         │
│  │  (新增返回、  │                          │  (操作按钮    │         │
│  │   搜索按钮)   │                          │   直接可见)   │         │
│  └──────────────┘                          └──────────────┘         │
│                                                                      │
│  ┌──────────────────────────────────────────────┐                    │
│  │          <<新增>> 短信便签管理                │                    │
│  │  · 收到短信自动创建便签                       │                    │
│  │  · 发送短信自动创建便签                       │                    │
│  │  · 查看短信便签文件夹                         │                    │
│  │  · 编辑短信便签内容                           │                    │
│  └──────────────────────────────────────────────┘                    │
│                                                                      │
│  ┌──────────────────────────────────────────────┐                    │
│  │          <<增强>> 通话便签管理                │                    │
│  │  · 来电/去电自动创建便签（标准 Android）       │                    │
│  │  · 通话记录实时监听（ContentObserver）         │                    │
│  └──────────────────────────────────────────────┘                    │
│                                                                      │
│  ┌──────────────────────────────────────────────┐                    │
│  │          <<修复>> 闹钟提醒                    │                    │
│  │  · 设置便签提醒时间                           │                    │
│  │  · 权限引导（通知/精确定时/全屏通知）          │                    │
│  │  · 锁屏闹钟弹窗                               │                    │
│  │  · 开机自动恢复闹钟                           │                    │
│  └──────────────────────────────────────────────┘                    │
│                                                                      │
│  ┌──────────────────────────────────────────────┐                    │
│  │          <<修复>> 搜索便签                    │                    │
│  │  · 实时搜索建议（随输入出现）                  │                    │
│  │  · 搜索结果列表对话框                         │                    │
│  │  · 搜索词高亮显示                             │                    │
│  └──────────────────────────────────────────────┘                    │
│                                                                      │
└──────────────────────────────────────────────────────────────────────┘
```

#### 代码缺陷描述

需要修复的代码缺陷（来自代码审查报告和相关分析）：

| 序号 | 严重程度 | 缺陷描述 | 涉及文件 |
|------|----------|----------|----------|
| 1 | 高 | `searchSuggestAuthority` 授权名错误，搜索建议失效 | searchable.xml |
| 2 | 高 | `ACTION_SEARCH` Intent 未处理，搜索回车无结果 | NoteEditActivity.java |
| 3 | 高 | 闹钟缺少 NotificationChannel，通知无法显示（API 26+） | AlarmReceiver.java |
| 4 | 高 | 闹钟 Activity 使用废弃 API（KeyguardLock/WakeLock），锁屏不显示 | AlarmAlertActivity.java |
| 5 | 高 | 缺少运行时权限请求（POST_NOTIFICATIONS/SCHEDULE_EXACT_ALARM/USE_FULL_SCREEN_INTENT） | NoteEditActivity.java |
| 6 | 中 | `WorkingNote.isWorthSaving()` 因 mContent 为空导致通话/短信便签保存失败 | CallReceiver.java, SmsReceiver.java |
| 7 | 中 | `Contact.getContact()` 缺少权限保护，导致文件夹点击崩溃 | NoteItemData.java |
| 8 | 中 | 菜单文件缺少 `showAsAction` 属性，按钮被隐藏 | 6 个 menu/*.xml |
| 9 | 中 | `sub_folder.xml` 缺少返回和搜索菜单项 | sub_folder.xml |
| 10 | 中 | `ListEditState` 缺少短信文件夹状态 | NotesListActivity.java |
| 11 | 低 | AndroidManifest.xml XML 格式错误 | AndroidManifest.xml |
| 12 | 低 | search_result.png 仅存在于 hdpi 目录 | res/drawable-*/ |

## 维护设计和实现方案

### 3.1 开源软件体系结构的设计及调整

本维护方案在保持原有分层架构（UI 层 → Model 层 → Data 层 + Tool 层 + GTask 层 + Widget 层）不变的前提下，对各层进行了以下增补和调整：

#### 体系结构类图

```
┌────────────────────────────────────────────────────────────────────┐
│                         UI Layer (ui/)                             │
│                                                                    │
│  NotesListActivity ◄──── NoteEditActivity ◄──── AlarmAlertActivity│
│  │ [+CallLogObserver]    │ [+checkAlarmPermissions()]              │
│  │ [+SmsSentObserver]    │ [+handleSearchIntent()]                 │
│  │ [+checkCallLogFor...] │ [+showPermissionGuideDialog()]          │
│  │ [+checkSentSmsFor...] │ [+onRequestPermissionsResult()]         │
│  │ [新增SMS_RECORD_FOLDER状态]                                     │
│  │                         │                                       │
│  CallReceiver [新增]      SmsReceiver [新增]                       │
│  AlarmReceiver [重写]     AlarmInitReceiver [修复]                 │
│  NoteItemData [修改]      NotesListItem [修改]                     │
├────────────────────────────────────────────────────────────────────┤
│                       Model Layer (model/)                         │
│                                                                    │
│  Note.java [修改]              WorkingNote.java [修改]             │
│  │ +mSmsDataId                 │ +convertToSmsNote() [新增]       │
│  │ +mSmsDataValues             │ +setWorkingText() 调用确保        │
│  │ +setSmsData()    [新增]     │   isWorthSaving() 通过            │
│  │ +mSmsDataValues [新增]      │                                  │
├────────────────────────────────────────────────────────────────────┤
│                        Data Layer (data/)                          │
│                                                                    │
│  Notes.java [修改]                 NotesDatabaseHelper.java [修改] │
│  │ +SmsNote 内部类 [新增]         │ DB版本 4→5                     │
│  │   - SMS_DATE                   │ +upgradeToV5()     [新增]     │
│  │   - PHONE_NUMBER               │ ID_SMS_RECORD_FOLDER 初始化    │
│  │   - SMS_BODY                   │                                │
│  │ +DataConstants.SMS_NOTE [新增] │                                │
│                                                                    │
│  NotesProvider.java               Contact.java                     │
│  (无需修改，已有搜索URI支持)        (无需修改，仅try-catch保护)      │
├────────────────────────────────────────────────────────────────────┤
│                        Tool Layer (tool/)                          │
│                                                                    │
│  DataUtils.java [修改]                                             │
│  │ +getSmsNumberByNoteId()          [新增]                        │
│  │ +getNoteIdByPhoneNumberAndSmsDate() [新增]                     │
└────────────────────────────────────────────────────────────────────┘

图例说明：
  [新增] = 新增的类/方法/属性
  [修改] = 修改了原有类
  [重写] = 大幅重写原有类
  [修复] = 修复了原有类的缺陷
```

#### 主要体系结构调整说明

**1. Data 层调整**

- `Notes.java`：新增 `SmsNote` 内部数据模型类，定义短信便签的数据列映射（SMS_DATE → DATA1, PHONE_NUMBER → DATA3, SMS_BODY → DATA4）和 MIME 类型常量
- `NotesDatabaseHelper.java`：数据库版本从 4 升级至 5，新增 `upgradeToV5()` 方法创建短信系统文件夹（`ID_SMS_RECORD_FOLDER = -4`）
- `DataUtils.java`：新增 2 个短信查询方法用于去重和展示

**2. Model 层调整**

- `Note.java`：内部类 `NoteData` 新增短信数据存储（`mSmsDataId`、`mSmsDataValues`）和 `setSmsData()` 方法，`pushIntoContentResolver()` 中新增短信类型的数据持久化逻辑
- `WorkingNote.java`：新增 `convertToSmsNote()` 方法，仿照 `convertToCallNote()` 实现，将工作笔记转换为短信便签类型

**3. UI 层调整**

- `NotesListActivity.java`：
  - `ListEditState` 枚举新增 `SMS_RECORD_FOLDER` 状态
  - `ROOT_FOLDER_SELECTION` 新增 `ID_SMS_RECORD_FOLDER` 的 OR 条件
  - 新增 `CallLogObserver` 内部类（监听通话记录数据库变化）
  - 新增 `SmsSentObserver` 内部类（监听发送短信数据库变化）
  - 新增 `checkCallLogForNewCalls()` 和 `checkSentSmsForNewMessages()` 补检方法
  - 新增 `registerCallLogObserver()` 和权限请求逻辑
- `NoteEditActivity.java`：
  - 新增 `checkAlarmPermissions()` 三层权限检查方法
  - 新增 `showPermissionGuideDialog()` 引导用户开启系统权限
  - 新增 `onRequestPermissionsResult()` 处理运行时权限回调
  - 新增 `handleSearchIntent()` 处理 `ACTION_SEARCH` Intent
  - `setReminder()` 前增加权限前置检查
- `CallReceiver.java`：新增的 BroadcastReceiver，监听 `PHONE_STATE` 和 `NEW_OUTGOING_CALL` 广播
- `SmsReceiver.java`：新增的 BroadcastReceiver，监听 `SMS_RECEIVED` 广播并解析 PDU
- `AlarmReceiver.java`：从直接 startActivity 重写为 Notification + FullScreenIntent 模式
- `AlarmAlertActivity.java`：添加 `DataExtractionRules` 兼容和 Notification 展示逻辑
- `NoteItemData.java`：支持短信文件夹数据读取，联系人查询加 try-catch 保护
- `NotesListItem.java`：支持短信列表项展示

**4. 资源层调整**

- `sub_folder.xml`：新增 `menu_back`（返回）和 `menu_search`（搜索）菜单项
- `note_edit.xml`、`call_note_edit.xml`、`note_list.xml` 等：所有菜单项添加 `showAsAction="ifRoom"`
- `sms_note_edit.xml`：新建短信便签编辑菜单
- `sms_record_folder.xml`：新建短信文件夹菜单
- `searchable.xml`：修正 `searchSuggestAuthority` 为 `micode_notes`
- `strings.xml`（3 种语言）：新增搜索无结果、短信便签相关字符串
- `search_result.png`：复制到 mdpi、xhdpi、xxhdpi 密度目录

### 3.2 维护需求的实现方案

以下通过每个新增/修复用例的顺序图，详细说明各项维护需求如何通过各个类对象之间的交互来完成。

#### （1）修复隐藏菜单栏和操作按钮

```
用户       NotesListActivity    MenuInflater    ActionBar
 │               │                  │              │
 │  打开APP      │                  │              │
 │──────────────>│                  │              │
 │               │ onPrepareOptionsMenu()          │
 │               │──────────────────>              │
 │               │ inflate(note_list)              │
 │               │                  │              │
 │               │ 读取 showAsAction="ifRoom"      │
 │               │─────────────────────────────────>│
 │               │                  │   渲染工具栏按钮
 │               │                  │   [新建文件夹][导出][设置][搜索]
 │               │<─────────────────────────────────│
 │  看到完整菜单  │                  │              │
 │<──────────────│                  │              │
```

**说明**：原菜单文件缺少 `android:showAsAction="ifRoom"`，Android 系统默认将菜单项折叠入溢出菜单。通过在所有 `<item>` 标签中添加此属性，使按钮直接显示在 ActionBar 上。

#### （2）修复闹钟提醒功能

```
用户         NoteEditActivity  AlarmManager  NotificationManager  AlarmReceiver  AlarmAlertActivity
 │                │                │              │                  │               │
 │ 点击"提醒我"   │                │              │                  │               │
 │───────────────>│                │              │                  │               │
 │                │checkAlarmPermissions()         │                  │               │
 │                │──┐             │              │                  │               │
 │                │  │ POST_NOTIFICATIONS?          │                  │               │
 │                │  │ → requestPermissions()      │                  │               │
 │                │  │ SCHEDULE_EXACT_ALARM?       │                  │               │
 │                │  │ → showPermissionGuideDialog()                 │               │
 │                │  │ USE_FULL_SCREEN_INTENT?     │                  │               │
 │                │  │ → showPermissionGuideDialog()                 │               │
 │                │<─┘             │              │                  │               │
 │                │ 全部通过 → show DateTimePickerDialog              │               │
 │                │──────────────────────────────────────────────────>│               │
 │ 设置时间确认    │                │              │                  │               │
 │<───────────────│                │              │                  │               │
 │                │ AlarmManager.setExactAndAllowWhileIdle()         │               │
 │                │───────────────>│              │                  │               │
 │                │                │  注册完成     │                  │               │
 │                                                                                  │
 │  [闹钟到期]                                                       │               │
 │                │                │              │  onReceive()     │               │
 │                │                │              │<─────────────────│               │
 │                │                │              │  创建Notification│               │
 │                │                │              │  (NotificationChannel)           │
 │                │                │              │  setFullScreenIntent()           │
 │                │                │              │──────────────────────────────────>│
 │                │                │              │                  │  onCreate()   │
 │                │                │              │                  │  锁屏弹窗     │
 │ 看到闹钟弹窗    │                │              │                  │               │
 │<───────────────────────────────────────────────────────────────>│               │
```

#### （3）修复通话记录便签（双重保障机制）

```
系统拨号器   CallLog.Calls DB   CallLogObserver  NotesListActivity  CallReceiver  WorkingNote
 │               │                  │                  │               │              │
 │ 用户挂断电话   │                  │                  │               │              │
 │──────────────>│                  │                  │               │              │
 │               │ 写入通话记录      │                  │               │              │
 │               │──┐               │                  │               │              │
 │               │<─┘               │                  │               │              │
 │               │ notifyChange()   │                  │               │              │
 │               │─────────────────>│                  │               │              │
 │               │                  │ onChange()       │               │              │
 │               │                  │─────────────────>│               │              │
 │               │                  │                  │checkCallLogFor│              │
 │               │                  │                  │NewCalls()     │              │
 │               │                  │                  │──┐            │              │
 │               │                  │                  │  │ 查询最新通话│              │
 │               │                  │                  │  │ date>last   │              │
 │               │                  │                  │  │ _call_log   │              │
 │               │                  │                  │  │ _check      │              │
 │               │                  │                  │<─┘            │              │
 │               │                  │                  │ createCallNote(phoneNumber)    │
 │               │                  │                  │──────────────────────────────>│
 │               │                  │                  │               │              │
 │               │                  │                  │               │ createEmptyNote()
 │               │                  │                  │               │─────────────>│
 │               │                  │                  │               │ convertToCallNote()
 │               │                  │                  │               │─────────────>│
 │               │                  │                  │               │ setWorkingText()
 │               │                  │                  │               │─────────────>│
 │               │                  │                  │               │ saveNote()   │
 │               │                  │                  │               │─────────────>│
 │               │                  │                  │               │ <───写入DB───│
 │  回到App       │                  │                  │               │              │
 │───────────────>│                  │                  │               │              │
 │ 看到通话便签   │                  │                  │ onResume()    │              │
 │<───────────────────────────────────────────────────│ 补检确认       │              │

[备用路径：CallReceiver 通过 PHONE_STATE 广播触发（老设备，API < 34）]
```

#### （4）新增短信记录便签

```
短信应用      SMS DB      SmsReceiver    NotesListActivity   WorkingNote    Note.java
 │              │              │                │               │              │
 │ 收到短信      │              │                │               │              │
 │─────────────>│              │                │               │              │
 │              │  发送广播      │                │               │              │
 │              │─────────────>│                │               │              │
 │              │              │ onReceive()    │               │              │
 │              │              │──┐ 解析PDU     │               │              │
 │              │              │  │ 提取发件人   │               │              │
 │              │              │  │ 提取正文     │               │              │
 │              │              │  │ 提取时间戳   │               │              │
 │              │              │<─┘             │               │              │
 │              │              │ getNoteIdByPhoneNumberAndSmsDate()         │
 │              │              │ (去重检查)      │               │              │
 │              │              │ createSmsNote()│               │              │
 │              │              │──────────────────────────────>│              │
 │              │              │                │ createEmptyNote()          │
 │              │              │                │─────────────>│              │
 │              │              │                │ convertToSmsNote()         │
 │              │              │                │ (phoneNumber,              │
 │              │              │                │  smsDate, smsBody)         │
 │              │              │                │─────────────>│              │
 │              │              │                │ setWorkingText()           │
 │              │              │                │─────────────>│              │
 │              │              │                │ saveNote()   │              │
 │              │              │                │─────────────>│              │
 │              │              │                │              │ syncNote()   │
 │              │              │                │              │─────────────>│
 │              │              │                │              │ ContentValues│
 │              │              │                │              │ → ContentProvider
 │              │              │                │              │ → SQLite DB  │
 │                                                                           │
 │ [发送短信路径]                  │                │                          │
 │─────────────>│              │                │                          │
 │ 写入sent表    │              │                │                          │
 │ notifyChange  │              │                │                          │
 │              │──────────────>│                │                          │
 │              │              │ SmsSentObserver.onChange()                 │
 │              │              │────────────────>│                          │
 │              │              │                │ checkSentSmsForNewMessages()
 │              │              │                │ (同样调用SmsReceiver.createSmsNote())
```

#### （5）修复搜索功能

```
用户        NotesListActivity  NoteEditActivity  NotesProvider  SQLite
 │                │                  │               │             │
 │ 点击搜索图标    │                  │               │             │
 │───────────────>│                  │               │             │
 │                │ onSearchRequested()              │             │
 │                │──────────────────>               │             │
 │ 输入文字        │                  │               │             │
 │ "hello"        │                  │               │             │
 │                │ [系统搜索框架查询]                │             │
 │                │──────────────────────────────────>             │
 │                │  query(search_suggest_query/hello)             │
 │                │                  │               │             │
 │                │                  │               │ LIKE '%hello%'
 │                │                  │               │────────────>│
 │                │                  │               │<───结果集────│
 │                │<───建议列表───────│               │             │
 │ 看到实时建议    │                  │               │             │
 │<───────────────│                  │               │             │
 │                                                                 │
 │ 按回车提交      │                  │               │             │
 │──────────────────────────────────>│               │             │
 │                │                  │ ACTION_SEARCH │             │
 │                │                  │ handleSearchIntent()        │
 │                │                  │──┐            │             │
 │                │                  │  │ 提取QUERY  │             │
 │                │                  │  │ 查询 /search?pattern=hello
 │                │                  │  │────────────────────────->│
 │                │                  │  │<───搜索结果──────────────│
 │                │                  │  │            │             │
 │                │                  │  │ 有结果 → AlertDialog列表 │
 │                │                  │  │ 无结果 → Toast "没有找到便签"
 │                │                  │<─┘            │             │
 │ 看到搜索结果    │                  │               │             │
 │<──────────────────────────────────│               │             │
```

### 3.3 维护需求的类设计

#### 新增类详细设计

**（1）CallReceiver（通话广播接收器）**

| 属性/方法 | 类型 | 说明 |
|---|---|---|
| `sPendingNumber` | `static String` | 缓存来电号码（响铃时记录，挂断后使用） |
| `TAG` | `static final String` | 日志标签 |
| `onReceive(Context, Intent)` | `void` | 接收 PHONE_STATE 和 NEW_OUTGOING_CALL 广播，分发处理 |
| `createCallNote(Context, String)` | `static void` | 静态方法，创建通话便签（去重→创建WorkingNote→转CallNote→写正文→保存） |

**（2）SmsReceiver（短信广播接收器）**

| 属性/方法 | 类型 | 说明 |
|---|---|---|
| `TAG` | `static final String` | 日志标签 |
| `onReceive(Context, Intent)` | `void` | 接收 SMS_RECEIVED 广播，解析 PDU，调用 createSmsNote() |
| `createSmsNote(Context, String, long, String)` | `static void` | 静态方法，创建短信便签（去重→创建WorkingNote→转SmsNote→写正文→保存） |

**（3）CallLogObserver（通话记录数据库观察者，内部类）**

| 属性/方法 | 类型 | 说明 |
|---|---|---|
| `onChange(boolean)` | `void` | 通话记录数据库变化时触发，调用 checkCallLogForNewCalls() |

**（4）SmsSentObserver（发送短信数据库观察者，内部类）**

| 属性/方法 | 类型 | 说明 |
|---|---|---|
| `onChange(boolean)` | `void` | 短信数据库变化时触发，调用 checkSentSmsForNewMessages() |

**（5）Notes.SmsNote（短信便签数据模型，内部类）**

| 属性/方法 | 类型 | 说明 |
|---|---|---|
| `SMS_DATE` | `static final String` | 短信日期字段，映射到 DATA1 |
| `PHONE_NUMBER` | `static final String` | 电话号码字段，映射到 DATA3 |
| `SMS_BODY` | `static final String` | 短信正文字段，映射到 DATA4 |
| `CONTENT_TYPE` | `static final String` | 目录 MIME 类型 |
| `CONTENT_ITEM_TYPE` | `static final String` | 条目 MIME 类型 |
| `CONTENT_URI` | `static final Uri` | Content URI |

#### 修改类新增/修改方法

**（1）NoteEditActivity 新增方法**

| 方法 | 说明 |
|---|---|
| `checkAlarmPermissions()` | 三层权限检查（POST_NOTIFICATIONS / SCHEDULE_EXACT_ALARM / USE_FULL_SCREEN_INTENT），返回 true 表示全部通过 |
| `showPermissionGuideDialog(String, String, String)` | 引导用户到系统设置开启特殊权限的对话框 |
| `onRequestPermissionsResult(int, String[], int[])` | 处理 POST_NOTIFICATIONS 运行时权限回调，通过后自动重新打开时间选择器 |
| `handleSearchIntent(Intent)` | 处理 ACTION_SEARCH Intent，查询数据库 → 构建 AlertDialog 结果列表 |

**（2）NotesListActivity 新增/修改属性和方法**

| 属性/方法 | 说明 |
|---|---|
| `mLastProcessedCallId` 等 | 通话记录去重标记 |
| `mCallLogObserver` | CallLogObserver 实例 |
| `mSmsSentObserver` | SmsSentObserver 实例 |
| `last_call_log_check` / `last_sms_check` | SharedPreferences 时间戳用于去重 |
| `registerCallLogObserver()` | 注册 CallLog ContentObserver |
| `checkCallLogForNewCalls()` | 扫描新增通话记录并创建便签 |
| `checkSentSmsForNewMessages()` | 扫描新增发送短信并创建便签 |
| `requestPhonePermission()` | 扩展为同时请求 READ_PHONE_STATE、READ_CALL_LOG、READ_CONTACTS、READ_SMS |

**（3）WorkingNote 新增方法**

| 方法 | 说明 |
|---|---|
| `convertToSmsNote(String, long, String)` | 将工作笔记转换为短信便签类型 |

**（4）Note.java 新增属性和方法**

| 属性/方法 | 说明 |
|---|---|
| `mSmsDataId` | 短信数据记录 ID |
| `mSmsDataValues` | 短信数据 ContentValues 差分集 |
| `setSmsDataId(long)` | 设置短信数据 ID |
| `setSmsData(String, String)` | 设置短信数据键值对 |

**（5）DataUtils.java 新增方法**

| 方法 | 说明 |
|---|---|
| `getSmsNumberByNoteId(ContentResolver, long)` | 根据便签 ID 查询短信电话号码 |
| `getNoteIdByPhoneNumberAndSmsDate(ContentResolver, String, long)` | 根据号码+日期查便签 ID（去重） |

**（6）NotesDatabaseHelper 新增方法**

| 方法 | 说明 |
|---|---|
| `upgradeToV5(SQLiteDatabase)` | V4→V5 升级：创建短信系统文件夹 |

## 维护后的开源软件

### 维护后的开源软件功能及演示

维护后的 NotesMaster 应用具备以下完整功能：

**（1）完整的笔记编辑工具栏**

进入笔记编辑界面后，工具栏直接显示所有操作按钮：新建（📝）、删除（🗑）、字体大小（A）、清单模式（☑）、分享（↗）、发送到桌面（📌）、提醒（⏰）。用户可以一键操作，无需进入溢出菜单。

**（2）完整的文件夹导航工具栏**

在根列表和子文件夹中，工具栏直接显示：新建文件夹（📁）、导出文本（💾）、设置（⚙）、搜索（🔍）。进入子文件夹后工具栏显示：返回（←）、新建笔记（📝）、搜索（🔍）。

**（3）闹钟提醒功能**

用户点击"提醒我"后，系统自动检查并引导用户授权所需权限：
- `POST_NOTIFICATIONS`：系统标准权限弹窗
- `SCHEDULE_EXACT_ALARM`：引导到系统设置页面开启"闹钟和提醒"权限
- `USE_FULL_SCREEN_INTENT`：引导到系统设置页面开启"全屏通知"权限

全部授权后，用户设置闹钟时间，到时间后手机锁屏状态下也会弹出全屏通知，显示便签内容，用户可点击"去看看"直接进入笔记。

**（4）通话便签自动创建**

用户在标准 Android 手机上拨打或接听电话后，返回应用即可看到"通话便签"文件夹出现在列表中。文件夹内有本次通话的便签，显示联系人姓名（需通讯录权限）和通话时间。点击文件夹查看通话列表，每条便签正文显示通话时间。

**（5）短信便签自动创建**

用户收到短信后，`SmsReceiver` 自动创建短信便签，包含发件人姓名（或号码）、短信时间和短信正文预览。用户发送短信后，`SmsSentObserver` 同样自动创建出发短信便签。返回应用后可看到"短信便签"文件夹和其中所有短信记录。

**（6）全文搜索功能**

用户点击搜索图标后，输入搜索关键词，系统实时显示匹配的笔记标题作为搜索建议。点按建议直接打开笔记并高亮搜索词。按回车提交后显示结果列表对话框，点按任意结果打开对应笔记。无匹配结果时友好提示"没有找到便签"。

### 对新增或修改的代码质量进行分析

#### 代码质量总体评价

维护过程中新增和修改的代码遵循了原有项目的编码风格和设计模式，整体质量良好：

**1. 代码风格一致性**

- 新增类（`CallReceiver`、`SmsReceiver`）完全遵循原有命名规范：成员变量使用 `m` 前缀，静态变量使用 `s` 前缀，常量使用 `UPPER_SNAKE_CASE`，日志 TAG 使用 `class.getSimpleName()` 方式
- 代码缩进、大括号风格、注释格式与原有代码保持一致
- 新增字符串资源遵循三种语言（英文/简体中文/繁体中文）的本地化规范

**2. 设计模式复用**

- 短信便签功能完全仿照通话便签的双重保障模式（BroadcastReceiver + ContentObserver），保持了设计一致性
- `CallLogObserver` 和 `SmsSentObserver` 遵循相同的 ContentObserver 模式（时间戳去重 + onResume 补检）
- 权限请求采用分层处理模式（标准运行时权限走系统弹窗，特殊权限走引导设置）

**3. 错误处理改进**

- `NoteItemData.java` 中联系人查询增加 try-catch 保护，防止权限缺失导致的崩溃
- `CallReceiver.createCallNote()` 使用 `TextUtils.isEmpty()` 处理空白号码
- `handleSearchIntent()` 对空搜索词和空游标均有防御性处理

**4. 可维护性**

- 新增方法职责单一，每个方法不超过 50 行
- 关键逻辑添加了详细的 Log.w() 日志输出，便于调试追踪
- 数据库升级路径清晰（V4→V5），使用 `insertWithOnConflict(CONFLICT_IGNORE)` 保证幂等性

#### 代码量统计

| 类别 | 定性和定量描述 |
| --- | --- |
| 开源软件原先代码量 | 约 10,001 行 Java 代码（39 个 Java 源文件），104 个资源文件 |
| 标注的代码行数量 | 维护后约 12,800 行 Java 代码（41 个 Java 源文件），112 个资源文件 |
| 新增的代码量 | 约 2,800 行 Java 代码（新增 CallReceiver.java、SmsReceiver.java 两个源文件；对 10 个已有源文件进行方法级增补），8 个新增资源文件（sms_note_edit.xml、sms_record_folder.xml 等） |
| 新增代码中重用的代码量 | 约 1,200 行。短信便签功能以通话便签为模板进行仿写，ContentObserver 模式复用相同的去重逻辑，数据库升级方法复用原有升级框架 |
| 新增代码中独立编写的代码量 | 约 1,600 行。主要包括：闹钟权限三层检查系统（checkAlarmPermissions + showPermissionGuideDialog + onRequestPermissionsResult）、搜索 Intent 处理（handleSearchIntent 完整流程）、SmsReceiver 的 PDU 解析逻辑 |

#### 代码缺陷修复情况

维护过程中修复了代码审查报告中指出的以下缺陷：

| 原缺陷 | 修复方式 |
|---|---|
| `getType()` 方法未实现 | 未在本次维护范围内修改（不影响核心功能） |
| `increaseNoteVersion` SQL 注入风险 | 未在本次维护范围内修改（该路径在现有使用场景下参数可控） |
| `Contact.getContact()` Cursor 泄露 | 新增 try-catch 保护，即使异常也能防止崩溃 |
| 常量拼写错误 | 未在本次维护范围内修改（API 已固定，修改会导致兼容性问题） |
| TAG 声明风格不一致 | 新增类统一使用 `class.getSimpleName()` |
| 线程安全问题 | 新增的 Observer 回调均在主线程执行，不引入新的线程安全问题 |
| 广泛捕获 Exception | 新增代码未使用空 catch，均有日志输出 |
| 缺少输入参数 null 校验 | 新增方法均在入口处进行 null/empty 检查 |

维护后的代码在原有良好设计基础上，增强了功能的完整性和健壮性。代码审查报告中评分为 7.9/10 的综合质量，在维护后预计可提升至约 8.5/10（新增功能代码质量良好、关键缺陷得到修复、错误处理更加完善）。

---

## 附录：项目部署步骤

> 以下步骤来自 CSDN 博客 [《小米便签项目部署》](https://blog.csdn.net/li1284780070/article/details/143580983)，笔者按照此步骤完成了项目的初始部署。共 8 条，按实际部署顺序整理如下。

### 1. 下载源码

从 GitHub 开源仓库下载小米便签（MiCode Notes）源码：

- 仓库地址：`https://github.com/MiCode/Notes`
- 操作：点击 "Code" → "Download ZIP"，下载 ZIP 压缩包
- 将下载的 `Notes-master.zip` 解压，得到 `Notes-master` 项目文件夹

### 2. 使用 Android Studio 打开项目

根据 Android Studio 版本选择合适的方式：

**方式一：直接导入（适用于旧版 IDE）**

启动 Android Studio → **File** → **New** → **Import Project** → 选择解压后的 `Notes-master` 文件夹。原项目是 Eclipse 工程结构，IDEA 支持自动识别和转换。

**方式二：新建空项目再迁移源码（适用于 Android Studio 2023+）**

1. 新建项目：**File** → **New** → **New Project** → 选择 **Empty Views Activity**
2. **包名**必须设为 `net.micode.notes`
3. **Language** 选择 **Java**，SDK 建议 API 30~34
4. 将解压后的 `Notes-master/src/net/micode/notes` 下的所有包复制到新建项目的 `app/src/main/java/net/micode/notes` 中
5. 将 `Notes-master/res` 目录下的所有资源文件复制到项目的 `app/src/main/res` 中（重复文件选择覆盖）
6. 将原 `AndroidManifest.xml` 的内容合并到新项目的清单文件中

### 3. 配置 Gradle

**（1）修改 `gradle-wrapper.properties`**

将 Gradle 下载源改为国内镜像，加速下载：

```properties
distributionUrl=https\://mirrors.aliyun.com/macports/distfiles/gradle/gradle-8.7-bin.zip
```

**（2）修改 `settings.gradle`，在 `dependencyResolutionManagement` 的 `repositories` 块中添加阿里云镜像：**

```groovy
maven { url = uri("https://maven.aliyun.com/repository/central") }
maven { url = uri("https://maven.aliyun.com/repository/public") }
maven { url = uri("https://maven.aliyun.com/repository/google") }
maven { url = uri("https://maven.aliyun.com/repository/gradle-plugin") }
```

**（3）修改项目级 `build.gradle`（`:app` 模块）**

```groovy
apply plugin: 'com.android.application'

android {
    compileSdkVersion 33
    buildToolsVersion "33.0.2"
    useLibrary 'org.apache.http.legacy'

    defaultConfig {
        applicationId "net.micode.notes"
        minSdkVersion 24
        targetSdkVersion 33
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.txt'
        }
    }

    // 解决 META-INF/DEPENDENCIES 重复文件冲突
    packagingOptions {
        exclude 'META-INF/DEPENDENCIES'
    }
}

dependencies {
    implementation files("你的本地路径/httpcomponents-client-4.5.14-bin/lib/httpclient-osgi-4.5.14.jar")
    implementation files("你的本地路径/httpcomponents-client-4.5.14-bin/lib/httpclient-win-4.5.14.jar")
    implementation files("你的本地路径/httpcomponents-client-4.5.14-bin/lib/httpcore-4.4.16.jar")
}
```

> **注意**：jar 包路径需要替换为本机的实际绝对路径。在笔者的环境中路径为 `D:\\CODE\\develop\\httpcomponents-client-4.5.14-bin\\lib\\`。

### 4. 下载并导入依赖库

项目依赖 `httpcomponents-client` 组件（用于 Google Tasks 同步的 HTTP 通信）：

- **下载地址**：https://hc.apache.org/downloads.cgi（下载 4.5.14 版本）
- 解压后，通过 **File → Project Structure → Dependencies → app → + → Jar Dependency**，依次导入 `lib` 目录下的以下 3 个 jar 文件：
  - `httpclient-osgi-4.5.14.jar`
  - `httpclient-win-4.5.14.jar`
  - `httpcore-4.4.16.jar`
- **不要**导入 lib 目录下的所有 jar，否则会出现 jar 包冲突

### 5. 解决常见编译错误

| 错误现象 | 原因 | 解决方法 |
|---|---|---|
| **控制台输出乱码** | IDE 默认编码与项目编码不一致 | 双击 Shift 搜索 `vmoption` → Edit Custom VM Options → 在文件末尾添加 `-Dfile.encoding=UTF-8` → 重启 Android Studio |
| **找不到符号 `setLatestEventInfo`** | 该方法在 Android 6.0（API 23）已废弃并在高版本 SDK 中移除 | 在 `GTaskASyncTask.java` 第 80 行附近，注释掉或删除调用 `setLatestEventInfo` 的代码 |
| **switch 语句报 `Constant expression required`** | 新版 Gradle 中 R 资源 ID 不再是 final 常量 | 在 `gradle.properties` 文件中添加：`android.nonFinalResIds=false` |
| **3 files found with path 'META-INF/DEPENDENCIES'** | 多个 jar 包中包含同名元数据文件 | 已在第 3 步的 `build.gradle` 中通过 `packagingOptions { exclude 'META-INF/DEPENDENCIES' }` 解决 |
| **Gradle 下载依赖缓慢或失败** | 默认 Maven 仓库为 Google 官方源，国内访问不稳定 | 已在第 3 步中配置阿里云镜像源 |

### 6. 同步或更新 Gradle 版本

如果 Gradle 版本不兼容，需手动调整：

- 推荐的 Gradle 版本范围：**7.6 ~ 8.7**
- 推荐的 Android Gradle Plugin（AGP）版本：**7.4.x ~ 8.2.x**
- JDK 版本要求：**JDK 11 及以上**

具体操作：
1. 修改 `gradle/wrapper/gradle-wrapper.properties` 中的 `distributionUrl` 为目标版本
2. 修改项目根目录 `build.gradle` 中的 AGP 版本
3. 点击 **Sync Project with Gradle Files** 重新同步

### 7. 编译构建

1. 点击工具栏的 **小锤子图标**（Make Module 'app'）进行编译
2. 等待 Gradle 构建完成，确认 **Build Output** 窗口显示 `BUILD SUCCESSFUL`
3. 若有编译错误，根据第 5 条中的方法逐一排查解决

### 8. 运行项目

1. 创建虚拟设备：**AVD Manager** → **Create Virtual Device** → 选择设备型号（如 Pixel 6） → 选择系统镜像（API 30+，推荐 API 33） → Finish
2. 或连接 Android 真机（需开启 USB 调试模式）
3. 点击绿色**运行按钮**（Run 'app'），选择目标设备
4. 等待 APK 安装完成，应用将在设备上自动启动

---

### 部署注意事项

- 由于小米便签开源代码编写年代较早（2010-2011），在新版 Android Studio（2023+）和 AGP 8.x+ 环境下会遇到较多兼容性问题，建议使用 AGP 7.4~8.2 范围
- Google Tasks 同步功能需要设备已登录 Google 账户并配置同步账户名
- 应用首次启动需要授予通话记录、短信、联系人等运行时权限
- 外部 jar 包引用使用绝对路径，换机部署时需调整 `build.gradle` 中的路径
